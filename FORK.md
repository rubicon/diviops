# FORK.md

This repository is a fork of [oaris-dev/diviops](https://github.com/oaris-dev/diviops).
This file is fork-owned. Upstream does not have it, so it never collides on a merge,
and it is the single record of how this fork differs and why.

## Why this fork exists

DiviOps Agent hardcodes the `divi/` block namespace throughout its block-targeting
code. `page_get_layout` is namespace-agnostic and will happily report a third-party
module as `difl/faq:1`, but every targeting path then refuses that same identifier.
The result is a read/write asymmetry: the plugin hands you a target it will not
accept.

The practical cost is that editing a third-party Divi 5 module's attributes requires
hand-reconstructing raw block markup. On a real page that corrupted 414
`$variable({...})$` tokens, which made the plugin's own validator reject every
subsequent write to that page.

This fork makes block targeting namespace-agnostic so third-party modules
(`difl/*`, `decm/*`, `d5bgo/*`) can be addressed by every operation, not just read.

A companion "bridge" plugin was built and abandoned first. It intercepted REST
responses and repaired them from outside. It was retired because it can only fix one
endpoint family at a time and structurally cannot fix Theme Builder insert, which
rejects third-party content with a 400 before any response a bridge could see.

## The drop-in constraint

**Four identifiers must never change.** They are the entire reason this fork loses
nothing relative to running stock DiviOps:

| Identifier | Value |
| ---------- | ----- |
| Plugin slug | `diviops-agent` |
| Main class | `DiviOps_Agent` |
| REST namespace | `diviops/v1` |
| Handshake filter | `diviops_agent_handshake_extensions` |

DiviOps Agent Pro is a separate commercial plugin that is **not** forked. It attaches
by calling `class_exists( 'DiviOps_Agent' )` and hooking
`diviops_agent_handshake_extensions`. The published MCP server — this fork's
`@rubicontv/diviops-mcp`, and equally upstream's `@diviops/mcp-server` — calls
`/diviops/v1/*` routes and gates each tool on the capability keys the handshake
returns.

Rename any of the four and Pro silently stops contributing its capabilities, and MCP
tools silently disappear rather than erroring, because the server's design is
"capability gate fails, tool is simply absent." The breakage is silent. Treat these
as frozen.

This is a deliberate exception to the maintainer's default `rtv_` vendor-prefix rule.
Preserving upstream identity is required for compatibility.

## Maintained-fork posture

Owner decision (2026-07-27): this is a **fork we own and maintain**, not a
divergence-minimizing upstream tracker. We set the version, roadmap, README, skills,
and release process. Upstream `oaris-dev/diviops` syncs from a private dev repo, takes
no outside PRs, and is treated as an occasional sync source we may cherry-pick from,
not something we actively track. We do not constrain edits to minimize divergence from
upstream.

Two things stay non-negotiable regardless of this posture: the four frozen identifiers
above (Pro + MCP compatibility), and honoring the fork base's license and attribution
to `oaris-dev`. The divergence tables below are retained as a record of what we've
changed relative to the fork base — history, and a reconciliation aid if we ever
cherry-pick an upstream change — not a constraint to keep divergence small.

Versioning follows the standard rubicon originating-repo baseline (release-please +
`CHANGELOG.md` + signed `vX.Y.Z` releases), not the manual-release pattern used by
upstream-tracking forks: this repo is a GitHub/rubicon repo and upstream ships no
release automation or changelog to collide with (#48).

## Divergence from upstream

Fork-owned files, added here, absent upstream. These never conflict on merge:

| Path | Purpose |
| ---- | ------- |
| `FORK.md` | This file |
| `CLAUDE.md` | Fork-owned agent instructions |
| `AGENTS.md` | Pointer stub to `CLAUDE.md` |
| `.gitignore` | Upstream ships none; ignores `worktrees/`, OS junk, and `diviops-server`'s Node build artifacts (`node_modules/`, `dist/`, `dist-test/`) |
| `.github/workflows/test.yaml` | Upstream ships no CI; adds the PHP lint/test jobs plus a `diviops-server` security-test lane (#27) |
| `tests/` | Upstream ships no tests |
| `plugins/diviops-agent/includes/trait-revision.php` | Fork-authored trait (`DiviOps_Agent_Revision`) adding the `revision` capability domain: read/diff/restore over WordPress's own native post revisions (`wp_get_post_revisions` / `wp_restore_post_revision`), complementary to the existing option-backed `rollback` snapshot store. A wholly new file, absent upstream, so it never conflicts on merge; wired into `diviops-agent.php` via a `require_once` + `use DiviOps_Agent_Revision;` alongside the other `trait-*.php`. `revision_list`/`_get`/`_diff` are `check_read_permission` + row-level `can_inspect_post_object` gated on the parent post; `revision_restore` is `check_write_permission` + `current_user_can('edit_post', parent)` gated, dry-run planned, and busts the Divi cache after a successful restore (#34) |
| `plugins/diviops-agent/includes/trait-media.php` | Fork-authored trait (`DiviOps_Agent_Media`) adding the `media` capability domain, absent upstream: `media_upload` (from a public URL, server-fetched, or from base64 bytes; exactly one of the two), `media_get`, `media_list`, `media_set_featured_image` (by existing attachment id or by uploading from a URL first), and `media_update_meta` (set/clear an attachment's alt text and/or caption; omitted field is left untouched, an explicit empty string clears it, idempotent no-op when the resulting values already match, dry-run supported) (#33). Security posture: URL uploads are SSRF-guarded — `media_url_is_safe()` rejects non-http(s) schemes and hosts that resolve to a reserved/private IPv4 or IPv6 range, and `media_fetch_to_temp()` re-runs that same check on every redirect hop (not just the caller's initial URL) with a bounded hop count, since WP's own auto-following `download_url()` would otherwise let a public origin 302 straight to a blocked target. `media_filetype_error()` validates the real file bytes via `wp_check_filetype_and_ext()` against the site's allowed mime types, rejecting an extension/content mismatch (type-spoofing) independent of the declared filename. SVG is rejected with `svg_sanitizer_required` unless `media_svg_sideload_sanitizer_active()` verifies Safe SVG's own callback (a `safe_svg` instance) is bound to `wp_handle_sideload_prefilter` — `has_filter()` alone would only prove something is listening, not that it sanitizes, so this scans `$wp_filter` for the specific class and fails closed when Safe SVG isn't installed. That sanitizer requirement is backed by a second, independent gate (#73): `media_svg_upload_error()` checks an admin-configurable, SVG-only capability BEFORE the sanitizer check, and neither gate substitutes for the other — a caller holding the capability still cannot upload an SVG without a verified sanitizer, and a site running Safe SVG still refuses a caller without the capability. `media_svg_upload_capability()` resolves it as `DIVIOPS_SVG_UPLOAD_CAPABILITY` wp-config constant > env var of the same name > the shipped default `upload_files`, deliberately mirroring the plugin's existing rate-limit settings surface; the default leaves #28 behavior byte-for-byte unchanged. NO filter seam is offered, unlike the rate limits' `diviops_rate_limits` — those are a tuning knob, this is a security gate, and a filter would let any plugin on the site lower it. The gate can only ever ADD a requirement, never remove one: `media_upload()` checks `upload_files` first, so configuring a weaker capability cannot unlock an upload for a caller who could not upload at all (covered by a test). `media_normalize_svg_capability()` resolves an unusable configured value (empty, non-string, or reduced to nothing by `sanitize_key`) to `do_not_allow` rather than back to the default, because answering an operator's typo in their own hardening with a silent return to the permissive setting is the one outcome a security control must not produce; `do_not_allow` is WordPress's own deny-everyone capability (`WP_User::has_cap()` excludes it from the multisite super-admin bypass and strips it from the resolved capability map, read directly on the reference install), which also makes it a documented kill switch for SVG uploads. Enforcement lives at the point where SVG is identified from the real bytes (`media_filetype_error()`), so it covers the URL-fetch and base64 paths with one site and cannot be evaded by a lying filename; a `dry_run` returns its plan without consulting either gate, since it fetches nothing and so has nothing to identify. Denial is `svg_capability_required` (403), reported ahead of the sanitizer check so a caller who may not upload SVG at all is not told which security plugins the site runs. Covered by `tests/test-media-svg-capability-gate.php`, including the denial, both-paths end-to-end refusal, the still-fails-closed-without-Safe-SVG case, non-SVG media being unaffected, and a child-process check that the wp-config constant really does beat the env var (a constant cannot be undefined once set, so testing that branch in-process would pin every later case). `media_get`/`media_list`/`media_update_meta` apply the same per-object `edit_post` read/write gate (`can_inspect_post_object()` / `query_inspectable_post_ids()` for the reads, direct `current_user_can('edit_post', $id)` for the `media_update_meta` write) as every other read/list/write handler, so a caller only sees or edits attachments it can edit. A wholly new file, absent upstream, so it never conflicts on merge; wired into `diviops-agent.php` via a `require_once` + `use DiviOps_Agent_Media;` alongside the other `trait-*.php` (#28, #33, #73) |
| `plugins/diviops-agent/includes/trait-dynamic-content.php` | Fork-authored trait (`DiviOps_Agent_DynamicContent`) adding the `dynamic_content` capability domain, absent upstream: `dynamic_content_list` (the live `divi_module_dynamic_content_options` registry — the same WordPress filter Divi's own `DynamicContentOptions::get_options()` runs, so it includes ACF/SCF fields that actually exist on this site, not a static catalog of the 55 `DynamicContentOption*` classes), `dynamic_content_build` (validates `name`/`settings` against that live registry, then emits the exact `$variable({...})$` token `Conversion::formatDynamicContent()` would — including the `(object)` cast so empty settings serialize as `{}` not `[]` — without writing anything), and `dynamic_content_validate` (validates a `name`+`settings` pair OR a raw token string, auto-detecting and decoding either the modern `$variable(...)$` form or the legacy D4 `@ET-DC@...@` form via `Conversion::DYNAMIC_CONTENT_REGEX`'s own base64/JSON shape; a legacy value additionally reports its exact modern `$variable()` equivalent). `dynamic_content_get_registry()` is memoized per `(post_id, context)` for the request. `module_update()` (`trait-page.php`) gained a write-path guard scoped to attrs values that look like dynamic content (`$variable(` prefix or an `@ET-DC@` substring, recursively, since a single dot-path can set a whole sub-tree) — but the guard (`dynamic_content_write_path_rejection()`) is deliberately far more lenient than `dynamic_content_validate`, because `$variable(...)$` is Divi's SHARED variable wrapper, not exclusive to dynamic content: global colors (`gcid-*`), global variables (`gvid-*` — spacing/sizes/fonts), and gradients use the identical syntax and are deliberately never registered in `divi_module_dynamic_content_options` (`DynamicContentGlobalVariableOptions::register_option_callback()` returns `$options` unchanged), and `type` does not discriminate (this fork's own canonical `gvid-` form uses `"type":"content"`, identical to a real dynamic-content binding). The guard fails OPEN on a malformed token, on the `gcid-`/`gvid-`/`gfid-` namespace or a non-`content` type, and on an empty/unavailable registry (a D4-only site, an older/deactivated Divi, or a non-REST invocation) — it rejects with `invalid_input` ONLY a well-formed token naming an option that is definitively absent from a non-empty registry. An ordinary plain string is never touched or rejected (#36). A wholly new file, absent upstream, so it never conflicts on merge; wired into `diviops-agent.php` via a `require_once` + `use DiviOps_Agent_DynamicContent;` alongside the other `trait-*.php` |
| `diviops-server/tsconfig.test.json`, `diviops-server/src/__tests__/` | Upstream ships no tests for `diviops-server`. A `node:test` suite scoped to `wp-cli.ts` (the WP-CLI command allowlist) and `wp-cli-fs-validator.ts` (the filesystem safe-root validator) — the two most security-relevant modules in the MCP server, previously untested (#27). Compiled via a dedicated `tsconfig.test.json` (outputs to `dist-test/`) rather than the project-wide `build`/`test` scripts, originally because `src/index.ts` failed to build for unrelated reasons (missing `cross-env-preflight` modules, fixed by #41) that would otherwise have blocked this suite from running at all; kept scoped this way regardless, since a security-relevant allowlist suite has no reason to depend on the rest of the server building |
| `diviops-server/src/cross-env-preflight/` (`.js`/`.d.ts` pairs + `README.md`), `diviops-server/scripts/copy-vendored-cross-env-preflight.mjs` | Fixes #41 — `src/index.ts` has always imported from `./cross-env-preflight/*`, but the `.ts` source was never present anywhere reachable, confirmed absent from this repo at every commit and from `oaris-dev/diviops`'s own public repo at both its `main` HEAD and its latest tag (`v1.5.49`); `npm run build` from a clean checkout failed with `ERR_MODULE_NOT_FOUND` as a result. This is Pro (`cross_env` module) preflight/fingerprint safety logic the fork cannot author without the real source — reimplementing a guess at it would be actively dangerous, not merely wrong, since a fabricated safe verdict on an unreviewed cross-environment write is worse than a build failure. Vendored instead from the published `@diviops/mcp-server@1.5.39` npm package's own compiled `dist/cross-env-preflight/` output — MIT-licensed, publicly distributed to every installer of that package, so this is using this project's own published distribution rather than a clean-room concern; unrelated to the separate, non-MIT `diviops-agent-pro` WordPress plugin this fork never reads from. Every vendored file's sha256 checksum is recorded and independently re-verified in `src/cross-env-preflight/README.md`, which also documents the safety rationale, provenance, and re-vendoring procedure. `copy-vendored-cross-env-preflight.mjs` exists because TypeScript (`allowJs` off) type-checks a `.js`/`.d.ts` pair via the `.d.ts` but does not emit the `.js` into `outDir` — confirmed directly: `npm run build` exits 0 with the copy step removed, and `dist/index.js` still fails at import time — so `package.json`'s `build` script now runs it immediately after `tsc` (#41) |
| `release-please-config.json`, `.release-please-manifest.json` | Upstream ships no release automation; release-please config + manifest for maintained-fork versioning — release-type `simple`, bare `vX.Y.Z` tags (`include-component-in-tag: false`), version bumped in `diviops-agent.php` via the generic updater's block/inline markers. The first fork release is bootstrapped to `1.6.0` via a one-time `Release-As: 1.6.0` commit footer on `main`, NOT the deprecated `release-as` config key (which would force that same version on every subsequent release); after the bootstrap, versions are conventional-commit-driven (#48) |
| `.github/workflows/release.yaml` | Upstream ships no CI; the release-please workflow. Runs under the org-wide `rubicon-release-please` GitHub App (verified/signed release commits), pulling the app credentials at runtime from 1Password so only `OP_SERVICE_ACCOUNT_TOKEN` is a repo secret (#48) |
| `CHANGELOG.md` | Upstream ships none; the fork's Keep-a-Changelog changelog, generated by release-please from Conventional Commits and curated per release (#48) |
| `skills/divi-5-builder/references/advanced-attributes.md`, `scripts/extract-decoration-paths.php` | Clean-room reference for Divi 5's seven "advanced" `module.decoration.*` families outside Tier 1's everyday border/background/spacing set — box shadow, filters, transform, sticky position, transition, scroll effects, and entrance animation — documenting canonical paths, value shapes, responsive/hover support, copy-paste `attrs` fragments, and provenance, in the same shape `module-formats.md` uses for the common set. `extract-decoration-paths.php` is the accompanying extraction helper: it reads Divi's own `Module/Options/<Group>/<Group>PresetAttrsMap.php` classes directly and prints the canonical `module.decoration.*` dot-paths each family declares, used to cross-verify every path documented in the reference. Both authored clean-room from Divi's own source only — the shared `PresetAttrsMap.php` classes, the matching `StyleLibrary/Declarations/<Group>/<Group>.php` CSS-emission classes, compiled Visual Builder JS (`visual-builder/build/*.js`), and this fork's own site for VB round-trip verification; `diviops-agent-pro` was never opened while authoring either file (#62). All 7 families now carry a `*(VB-verified)*` marker: boxShadow/transform/transition/sticky verified 2026-07-28 (#62); filters/scroll/animation promoted from `*(verified)*` to `*(VB-verified 2026-07-29)*` after round-tripping every documented subfield path (9/9 filters, 13/13 scroll, 12/12 animation) through `diviops_module_update` → `diviops_module_get` + an independent raw `wp post get` read on a scratch Text module (page 900592, trashed after), with no mismatches found — every path documented matched what was observed, so no path corrections were needed (#82). All verification for both passes went through the MCP write path against this fork's own site, not a literal browser Visual Builder session; `diviops-agent-pro` was not consulted for either (#62, #82) |
| `diviops-server/scripts/regen-module-formats.mjs`, `diviops-server/scripts/regen-module-formats.test.mjs` | Upstream ships neither the script nor tests for it. Mechanically regenerates `skills/divi-5-builder/references/module-formats.md`'s "Generated path index" section (per-module `{key}.decoration.{name}` dot-path bullets) from `GET /schema/module/dump-all`, replacing only the file's `BEGIN/END GENERATED:*` sentinel-bounded regions — reverse-engineered from #115's stopgap documentation of the algorithm after discovering that neither this script nor `diviops-server/CONTRIBUTING.md` (which the doc's own header pointed to) had ever been committed. Regenerates only module blocks the file already curates as sentinel pairs; adding a new module to the index is a manual sentinel-pair addition, not something the script auto-discovers (matching #63's "starter batch... expand iteratively" framing — the reference site has 84 native and 109 third-party structurally-eligible modules against the 28 currently curated). `regen-module-formats.test.mjs` covers the transform logic (schema JSON to markdown) via `node:test` with synthetic fixtures, modeled on real Divi module shapes but not copied from Divi's own `module.json`; verified live against the reference site, byte-identical output for all 28 currently-committed module blocks (#116) |
| `diviops-server/CONTRIBUTING.md` | Upstream ships no `diviops-server`-specific contributor doc (the repo-root `CONTRIBUTING.md` is upstream's own, unmodified here, and describes `oaris-dev/diviops`'s own sync process rather than this fork's). Documents the sentinel convention `regen-module-formats.mjs` depends on, how to run it, and the per-module generation algorithm — the "full convention" the doc's own header comment references (#116) |
| `scripts/extract-module-preset-paths.php`, `scripts/lib/preset-attrs-map.php`, `tests/test-preset-attrs-map-extractor.php`, `tests/fixtures/preset-attrs-map/` | Merge-aware per-module leaf-path extractor, the prerequisite the cached module-map artifact's source 3 is blocked on (`docs/superpowers/specs/2026-07-31-module-map-artifact-design.md`). A per-module `<Module>PresetAttrsMap::get_map( array $map, string $module_name )` both adds and removes keys: `CTAPresetAttrsMap.php` unsets 151 and merges 161 back, and the unset ones are paths CTA's own source says are invalid for CTA, sitting in the file as quoted strings a text scanner cannot tell from the valid ones. Order matters as much as membership, since unset runs before merge, so a key in both lists survives while a key in the unset list alone does not. Rather than model that, the resolver runs `get_map()` and reads the result — sound because a token scan of all 111 `PresetAttrsMap.php` files Divi 5.9.0 ships (65 `ModuleLibrary/`, 46 `Module/Options/`) found their entire vocabulary to be `array_merge`, `array_filter`, `in_array`, `substr`, `defined`, and static calls to sibling `*PresetAttrsMap` classes: no WordPress function, no I/O, no global state. Divi source is read-only reference throughout. Three cases behaviour alone cannot separate are handled explicitly rather than papered over: a map that only unsets (`divi/code` and seven others contribute no keys of their own) is probed with every path-shaped string in its own file so the keys it strips are observed being stripped and reported as `invalidates`; a map that returns an empty array (`divi/social-media-follow-network`) is caught by a sentinel key in that probe and reported as `wipes_base`; and five maps that match their module name and then return the map they were handed (`divi/canvas-portal`, `divi/group-carousel`, `divi/icon-list`, `divi/icon-list-item`, `divi/lottie`) are indistinguishable from a file that merely names a module, so those alone fall back to reading the `get_map()` guard, and are reported as `inert`. A file naming a module it does not serve is refused rather than reported as an empty result. Complements rather than replaces `extract-decoration-paths.php`, which answers a different question (the shared `module.decoration.*` vocabulary of the seven families `advanced-attributes.md` documents, by scanning source text) and is unchanged. Tests run against hand-authored fixtures with no Divi install; the real tree is exercised only when `DIVIOPS_DIVI_BUILDER5_PATH` points at one, so CI stays green without Divi. Verified against Divi 5.9.0 on the reference install: all 66 module names resolve, 16,336 keys across 151 distinct element prefixes, and CTA's 151 unset paths appear in none of them (#119) |
| `diviops-server/src/request-context.ts`, `diviops-server/src/__tests__/tool-cancellation.test.ts` | Fork-authored, absent upstream. `request-context.ts` is an `AsyncLocalStorage` carrying the MCP request's `AbortSignal` (and a per-tool transport-abort policy flag) from the tool-registration wrapper down to `WPClient`'s `fetch`, so cancellation reaches all ~145 plugin/pro/local tools instead of only the two meta tools upstream wired it to. Ambient rather than parameter-threaded because the alternative is a signal parameter on every handler and all 136 `wp.request`/`wp.requestEnveloped` call sites. `tool-cancellation.test.ts` is the tool-boundary regression suite: it installs the real finalized registry, invokes the real registered handlers with an SDK-shaped `extra`, and asserts against a recording `fetch` injected ahead of the module-level `WPClient` (#134) |
| `diviops-server/scripts/verify-server-builds-and-starts.test.mjs` | Fork-authored regression test for #41 and #128 — runs the real `npm run build` (not a narrower tsc invocation, which is exactly what let #41 stay invisible), removes `dist/cross-env-preflight/` first so a stale prior build can't mask a regression, asserts the vendored files land in `dist/`, then spawns `node dist/index.js` with no WP credentials and asserts it reaches the graceful "missing environment variable" refusal rather than an `ERR_MODULE_NOT_FOUND` or `_meta.idempotent` crash. Needs no live WordPress site |

Modified upstream files. Each carries fork changes reconciled by hand only if we ever
cherry-pick an upstream change (we do not actively track upstream — see Maintained-fork
posture):

| Path | What diverges | Issue |
| ---- | ------------- | ----- |
| `diviops-server/package.json` | Adds a `test:server-security` npm script (`tsc -p tsconfig.test.json && env -u DIVIOPS_WP_CLI_ALLOW node --test dist-test/__tests__/*.test.js`) that runs the new WP-CLI allowlist / filesystem path validator suite independently of the existing `build`/`test` scripts, so it isn't blocked by the unrelated `src/index.ts` build failure that predated #41 (#27). `regen:skill` (`node scripts/regen-module-formats.mjs`) was added by commit `1bcbea3`, the same commit that first wrote `module-formats.md`'s header claiming the tooling existed — #115's investigation found it never had been, and this change (#116) is what makes the script-target reference true. Adds `test:regen-skill` (`node --test scripts/regen-module-formats.test.mjs`) for the same reason `test:server-security` is separate: plain ESM, no `tsc` involved, so it isn't blocked by the `src/index.ts` build failure that predated #41 either (#116) | #27, #116 |
| `plugins/diviops-agent/diviops-agent.php` | Adds namespace-agnostic block-comment constants (`BLOCK_OPEN_PREFIX`, `BLOCK_CLOSE_PREFIX`, `BLOCK_NAME_PATTERN`, `DEFAULT_BLOCK_NS`). The `divi/`-specific `SECTION_OPEN`, `SECTION_CLOSE`, and `BLOCK_PREFIX` constants are retained, unused, because they are public class constants external code may reference (#2). Adds `GLOBAL_LAYOUT_BLOCK_NAME` for the `divi/global-layout` wrapper name every counting site checks against (#13). Registers `POST /variable/update` (admin-only, same permission callback as `/variable/create` and `/variable/delete`) and adds the `variable_update` capability key so the handshake advertises it (#25). Registers `POST /library/delete/(?P<id>\d+)` (admin-only, same permission callback as `/library/save`) and adds the `library_delete` capability key (#26). Adds an optional `post_type` arg (default `page`) to the `/page/create` route (#31). Registers the four menu-CRUD write routes (`POST /menu/delete/(?P<id>\d+)`, `POST /menu/item/remove`, `POST /menu/item/reorder`, `POST /menu/location/unassign`), all gated by the existing `check_menu_permission` callback, and adds the `menu_delete`, `menu_item_remove`, `menu_item_reorder`, and `menu_location_unassign` capability keys to the handshake (#30). Registers `POST /page/block-insert/(?P<id>\d+)` (write-gated, same `check_write_permission` as `/page/create`) and adds the `page_block_insert` capability key (#32). Adds release-please version markers so the maintained-fork release automation bumps both version locations: `x-release-please-start-version` / `x-release-please-end` block markers wrap the WP header `Version:` line (a block form, so no inline comment corrupts WP's header parser — verified WP still reads the value cleanly), and an inline `// x-release-please-version` marks `const VERSION` (#48). Registers the four native-revision routes (`GET /revision/list/(?P<id>\d+)`, `GET /revision/get/(?P<revision_id>\d+)`, `POST /revision/restore/(?P<revision_id>\d+)`, `GET /revision/diff`) — list/get/diff `check_read_permission`, restore `check_write_permission` — and adds the `revision_list`, `revision_get`, `revision_restore`, and `revision_diff` capability keys to the handshake (#34). Registers the four media routes (`POST /media/upload`, `GET /media/get/(?P<id>\d+)`, `GET /media/list`, `POST /media/set-featured-image`) — upload and set-featured-image gated by `check_authenticated_permission` (both mutate; set-featured-image adds its own per-object `edit_post` check inside the handler), get and list by `check_read_permission` — and adds the `media_upload`, `media_get`, `media_list`, and `media_set_featured_image` capability keys to the handshake (#28). Registers `POST /media/update/(?P<id>\d+)` (`check_authenticated_permission`, plus its own per-object `edit_post` check inside the handler, matching `media_set_featured_image`'s pattern) and adds the `media_update_meta` capability key (#33). Adds the `theme_options_update` capability key to `CAPABILITIES` so the handshake advertises the pre-existing `POST /theme-options` route (registered since before this fork, gated by the same `manage_options` check inside its handler, but never listed in `CAPABILITIES` — so `diviops_theme_options_update` (#29) would otherwise have no MCP tool to attach to) — the one-line mechanical addition the array's own docblock requires for any route a tool is added for; no route, handler, or permission behavior changes (#29). Registers the three dynamic-content routes (`GET /dynamic-content/list`, `POST /dynamic-content/build`, `POST /dynamic-content/validate`), all gated by `check_read_permission` (none of the three write anything), and adds the `dynamic_content_list`, `dynamic_content_build`, and `dynamic_content_validate` capability keys to the handshake (#36). Registers `POST /page/duplicate/(?P<id>\d+)` (write-gated, same `check_write_permission` as `/page/create`) and adds the `page_duplicate` capability key (#35). | #2, #13, #25, #26, #31, #30, #32, #48, #34, #28, #33, #29, #36, #35 Extracts the admin dashboard's capability list into a testable `dashboard_capabilities()` and drops its WP-CLI row, which reported a red cross on setups where WP-CLI worked: this plugin never executes WP-CLI (no `shell_exec`/`proc_open`/`passthru`/`WP_CLI::` anywhere in it), the Node MCP server does, and PHP cannot read that separate process's environment — so `getenv('WP_PATH')`/`getenv('WP_CLI_CMD')` could not see it, and the one lever that could (`DIVIOPS_WP_CLI_PATH`, a constant referenced nowhere else) would only have inverted the false negative into an unconditional false positive. Real WP-CLI readiness is reported by the MCP server via `diviops_meta_info`. Covered by `tests/test-dashboard-capabilities.php`, which also asserts the no-WP-CLI-execution premise so the reasoning cannot silently rot (#123). The row is then restored with real data rather than left absent: the MCP server reports its own WP-CLI readiness in the handshake it already performs (`client_runtime.wp_cli`, optional and additive so older servers handshake unchanged), the plugin records it with a timestamp and reporting server version, and `dashboard_client_runtime()` renders three states — available / unavailable / **unknown** — under a separate "Reported by MCP client" heading. `unknown` is the state the old binary row could not express and is why it showed a red cross on working setups. An absent `client_runtime` never overwrites a prior report, because silence is not a retraction (#123) |
| `plugins/diviops-agent/includes/trait-core.php` | Adds the `block_identifier_from_name()` / `block_name_from_identifier()` pair that defines the targeting-identifier contract, plus `next_block_opener()`. Makes the write-safety marker census, the marker-sequence validator, and block-attr normalization namespace-aware (#2). `block_opener_is_self_closing()` delegates to `block_opening_comment_end()` instead of a raw `strpos` for `-->`, so it no longer misreads a container as self-closing when an attribute value contains a `/-->`-shaped sequence (#6). Adds `counted_block_name()` / `counted_block_identifier()`, which resolve a `divi/global-layout` wrapper's counted type from its own `attrs.blockName` (falling back to the wrapper's literal name when that attr is absent), so every counting site agrees with what `page_get_layout` counts the wrapper as on read (#13). Adds the layered write-safety guard for the global-layout materialization hazard: `parse_blocks_for_write()` routes a write-path parse through Divi's own `BlockParserUtils::parse_blocks_with_layout_context( $content, 'saving_content' )` when available, falling back to plain `parse_blocks()` otherwise; `global_layout_wrapper_identities()` is a JSON-aware scan that reads each wrapper's `globalModule` id in document order (falling back to a `no_global_module_id_sentinel()` token for a wrapper with no readable id) and returns `null` — an untrustworthy scan — the moment any opener anywhere in the content can't be resolved to its own comment terminator; `global_layout_wrapper_drift()` compares the two sides' id multisets (identity-aware, not count-only, so a wrapper swapped for a different one is drift even though the overall count is unchanged) and treats a `null` scan on either side as drift, refusing the write rather than risking a malformed opener silently masking a real loss; `update_post_content_with_integrity_guard()` gains an opt-in `$check_global_layout_drift` parameter so only the parse/serialize round-trip call sites enforce it, not the raw-content writers that share the same function (#11). `global_layout_wrapper_drift()` is split into `global_layout_write_refusal_reason()`, which returns which of the two refusal reasons applies (`identity_lost` or `scan_unreliable`) instead of a single bool, so `update_post_content_with_integrity_guard()` can emit a distinct error code and message for each; `global_layout_wrapper_drift()` itself becomes a thin bool wrapper kept for `preset_reassign()`'s direct call (#23). `parse_blocks_for_write()`'s own docblock corrected: it previously described `_is_rest_update_request()`'s skip condition as a fragile `$_SERVER['REQUEST_URI']` string match, which is not what the real method does (`Conditions::is_rest_api_request()` plus a REQUEST_METHOD check) — confirmed by reading it directly on the reference Divi install (#99). Adds `block_round_trip_drift()`, the round-trip fidelity check: it returns `{ original_bytes, reserialized_bytes, byte_delta }` when a page's own `parse_blocks_for_write()` → `serialize_blocks()` is not byte-identical, and null when it is. Nothing else in the file catches that class of loss — `update_post_content_with_integrity_guard()` compares stored against requested, and a silently normalized re-serialization *is* what was requested — so a caller about to mutate a parsed tree serializes the unmodified parse first and refuses the page on drift. Byte comparison, not length comparison: a same-length rewrite is drift too (#188). | #2, #6, #11, #13, #23, #99, #188 |
| `plugins/diviops-agent/includes/trait-page.php` | Namespace-agnostic raw scanners in `module_update()` and `find_block()`, `*/section` matching in `find_all_sections()`, shared identifier derivation in `parse_block_tree()` and `walk_and_mutate()`, and namespace-agnostic parser-backed collectors for the `module_get` / `module_move` fallbacks (#2). Adds `block_opening_comment_end()`, a JSON-string-aware scan that keeps `find_block()` from truncating a module's span when a `-->` appears inside one of its attribute values (#5). Routes the remaining raw `strpos($content, '-->', $pos)` sites through that same helper: `find_block()`'s own container depth-scan, `module_update()`'s attribute-span scan, `extract_attrs_from_block_markup()`, and both the opening-comment scan and depth-scan in `find_all_sections()` — closing the class of bug where a descendant module's attribute JSON contains an ancestor's closing comment, or a block's own attribute JSON contains a `-->` (#6). `find_block()`, `module_update()`'s inline scanner, `find_all_sections()`, `parse_block_tree()`, and `walk_and_mutate()` all route their type/section resolution through `counted_block_name()` / `counted_block_identifier()`, so a `divi/global-layout` wrapper counts as the type it resolves to instead of counting literally as `global-layout:N` (#13). `collect_readable_divi_blocks()` (`module_get`'s parser fallback) and `collect_parser_move_blocks()` (`module_move`'s parser fallback) now route through the same `counted_block_identifier()` resolution, closing the one gap #13 left out of scope (#14). `find_all_sections()` now checks `is_self_closing` (via `block_opening_comment_end()`) before its own nesting depth-scan, mirroring `find_block()`: a self-closing section opener, wrapper or not, is a complete one-comment span rather than routed through the depth-scan, which previously consumed the enclosing (or a later) section's real closer and reported a bogus overlapping match (#12). `load_post_for_module_op()` (shared by `module_lock`/`module_unlock`/`module_clone`) and `move_block_with_parser()` (`module_move`'s parser fallback) now parse through `parse_blocks_for_write()` instead of bare `parse_blocks()`, and both writer call sites (`save_mutated_blocks()`, and `module_move()`'s own `update_post_content_with_integrity_guard()` call) pass `$check_global_layout_drift = true` (#11). `page_create()` gains an optional `post_type` param (default `page`): it reads and `sanitize_key()`s the param, rejects an unregistered type with `invalid_input` (a deliberate divergence from `page_list`, which silently falls back — creation must not silently retarget a write), and threads the resolved type into the dry-run plan, `wp_insert_post`, and `initialize_divi_page_meta()` (whose `_et_pb_built_for_post_type` meta now reflects the real type instead of a hardcoded `page`; the helper gains a `$post_type` param defaulting to `page`) (#31). Adds `page_block_insert()` — the page counterpart to `tb_layout_block_insert`, inserting a block (row/column/module) at a `parent_path` or `parent_selector` position (append/prepend/before/after) without rebuilding the section. It REUSES the generic tree helpers (`find_tb_block_by_path`/`find_tb_block_by_selector`/`apply_tb_block_insert`/`tb_insert_sequence_matches`/the stable-labeled-sequence checks — named `tb_*` by birthplace but not TB-specific) rather than duplicating them, and leaves the #11-critical TB handler untouched. Being a `parse_blocks_for_write()` → `serialize_blocks()` round trip, it passes `$check_global_layout_drift = true` to `update_post_content_with_integrity_guard()` so a page carrying a `divi/global-layout` wrapper cannot have it materialized by the write (#32). `page_update_content()` no longer re-stamps the Divi page meta on every content write: it gated the `initialize_divi_page_meta()` call behind a new `should_init_divi_page_meta_on_write()` (Divi content AND not already a Divi page) and passes the post's real `post_type`, fixing the bug where every edit reset `_et_pb_page_layout` to full-width and mis-keyed `_et_pb_built_for_post_type` to `page` on a non-page post (#45). `module_update()` gains a dynamic-content write-path guard (`dynamic_content_validate_module_update_attrs()` / `dynamic_content_write_path_rejection()`, `trait-dynamic-content.php`): only attrs values that look like dynamic content are inspected at all, and — since `$variable(...)$` is also Divi's shared global-color/global-variable/gradient wrapper, not exclusive to dynamic content — the guard fails open on anything malformed, on the `gcid-`/`gvid-`/`gfid-` namespace, and on an empty/unavailable registry, rejecting only a well-formed token naming an option definitively absent from a non-empty registry (#36). Adds `page_duplicate()` — same-site whole-page duplication (owner-approved split, issue #35 comment 2026-07-29): reuses `page_get_layout`'s read gate and `page_create`'s created-post response shape rather than reimplementing either. Deliberately ships with NO reference remapping (attachment ids / internal links / global color-font-variable refs are copied as-is because the target is the same site as the source, so nothing needs remapping there — cross-site remapping is out of scope, tracked separately as #96), and discloses that explicitly in the response via `references_remapped: false` plus a `references_note` rather than leaving a caller to assume more happened. A first version of this handler treated duplication as a `parse_blocks_for_write()` → `serialize_blocks()` round trip guarded by `update_post_content_with_integrity_guard( ..., true )`, the same discipline `page_block_insert()` (#32) uses. Live verification against a real Divi page (900390: 62KB, 23 block types, a genuine `divi/global-layout` wrapper) found that wrong: the write-safety validator that guarded path runs false-positived on a `u003c`-shaped escape Divi itself legitimately emits (refusing to duplicate a page that parses and renders fine), and even when it succeeded the round trip was lossy (62,167 → 61,855 bytes on that same page). Corrected to a byte copy: `wp_insert_post()` is called once with the source's raw `post_content` (`wp_slash()`-escaped, unmodified) plus `post_excerpt`/`post_parent`/`menu_order`, the same shape `canvas_duplicate()` (`trait-canvas.php`) already uses for the identical reason — duplication copies, it does not mutate a block tree, so it has no reason to parse one, which makes the `divi/global-layout` materialization hazard (#11) structurally impossible rather than merely guarded. The single insert also removes the empty-shell-page window the guarded version needed a cleanup path for. Also copies the `_wp_page_template` meta, the featured image (via `get_post_thumbnail_id()`/`set_post_thumbnail()`), and taxonomy term assignments (`wp_get_object_terms()` → `wp_set_object_terms()` per taxonomy registered on the source's post type). A non-Divi (classic-editor) source is duplicated too, not refused — the response reports `source_uses_divi` so a caller can tell which kind of content was copied — and Divi builder meta is only stamped on the copy via the existing `should_init_divi_page_meta_on_write()` gate (#45) when the copied content is actually Divi content, so a non-Divi source's duplicate is not force-converted into a Divi page (#35). | #2, #5, #6, #11, #12, #13, #14, #31, #32, #45, #36, #35 |
| `plugins/diviops-agent/includes/trait-module-schema.php` | Adds `is_divi_module_block()` so schema listing and dumping recognize third-party Divi modules, and accepts a namespaced name in `schema_get_module()` (#2). Adds native-module schema introspection: native Divi 5 core modules (`divi/text`, `divi/section`, …) are largely absent from `WP_Block_Type_Registry`, so `schema_get_module()` now falls back to reading Divi's own on-disk `module.json` (under `includes/builder-5/visual-builder/packages/module-library/src/components/<slug>/`, resolved via `get_theme_file_path()`), and `schema_list_modules()` augments its list from the same files. New helpers: `native_module_slug_from_name()` maps `divi/<slug>` to a dir slug and is the hard path-traversal guard (slug must be `^[a-z0-9]+(?:-[a-z0-9]+)*$`, so nothing with `.`/`/`/`..` reaches the filesystem); `native_module_schema_from_dir()` adds a realpath-containment check on top; `parse_native_module_json()` rejects a file whose declared `name` mismatches the request. Responses carry a `source` (`divi_module_json` vs `block_registry`). Live-verified on Divi 5.9 (native modules resolve with their attribute trees; traversal names return not_found) (#42). `schema_get_module_dump_all()` (the build-time bulk source for the skill-regen pipeline) now also merges native modules' full schemas via a `native_module_schemas_all_from_dir()` bulk read of the same `module.json` files (reusing `parse_native_module_json`), so the bulk dump is complete; registered entries win on a name collision (#61). `schema_get_module_dump_all()`'s docblock defined `schema_version` as SHA-1 over every `*PresetAttrsMap.php` file in `Packages/ModuleLibrary/`; the implementation has walked all of `Packages/` since the fork base, and `schema_preset_attrs_map_hash()`'s own docblock already said so and explained why (shared option maps under `Packages/Module/Options/` are imported by the per-module ones — `TextPresetAttrsMap.php:11` imports `LoopPresetAttrsMap`, confirmed on the reference install, where the walk covers 111 files: 65 under `ModuleLibrary`, 46 under `Module/Options`). The consumer-facing docblock is corrected to the real scope, and now also discloses the limitation the narrower claim obscured: the walk roots at `get_theme_file_path()`, the Divi theme, so a third-party module plugin update cannot move `schema_version` (zero `*PresetAttrsMap.php` files exist anywhere under `wp-content/plugins/` on the reference install). Docs only — the hashing behavior is unchanged, and was verified correct before the docs were touched. Guarded by `tests/test-schema-version-hash-scope.php`, which derives the walk root from the function body and asserts both documentation surfaces agree with it (#122). | #2, #42, #61, #122 |
| `plugins/diviops-agent/includes/trait-theme-builder.php` | Theme Builder insert accepts any namespaced block, `parse_tb_parent_selector()` accepts any namespace, the cross-env preset and attachment scanners no longer skip third-party blocks, and malformed-comment detection covers every namespace (#2). `tb_layout_block_insert()` parses the stored layout through `parse_blocks_for_write()` instead of bare `parse_blocks()`, and its `update_post_content_with_integrity_guard()` call passes `$check_global_layout_drift = true`. `parse_divi_blocks_for_insert()` also parses the caller-supplied INSERTION content through `parse_blocks_for_write()` — a second write-path parse in the same operation that the initial version of this fix missed, since a wrapper arriving in the inserted content itself is expanded before it ever reaches the tree, ahead of anything the drift-guard's baseline could compare against. `tb_layout_update()` (a raw-content write, not a parse/serialize round trip) is unchanged (#11). `normalize_and_validate_divi_markup_before_write()` (backing `trait-page.php`'s `final_page` and this file's `final_layout`/per-field theme-builder writes) had drifted from the established pattern and parsed via bare `parse_blocks()` for its own validation walk — found while investigating #97, confirmed via `wp eval-file` (materializes) vs. a real REST call (does not) against page 900390, fixed to `parse_blocks_for_write()` (#99). | #2, #11, #99 |
| `plugins/diviops-agent/includes/trait-preset.php` | `preset_reassign()` parses through `parse_blocks_for_write()` instead of bare `parse_blocks()`. It calls `global_layout_wrapper_drift()` directly before the write, refusing only the affected page — with an error recorded in the batch summary — rather than the whole batch (#11). Five write-safety defects fixed in the plugin's only site-wide write (#188): the batch became a **loop over guarded single writes** through `update_post_content_with_integrity_guard()`, replacing the bare `wp_update_post()` it used on the earlier reading that a batch cannot fit that function's single-post readback/revert contract — giving up per-page readback and auto-revert was giving up the only real atomicity available here. Each applied page now takes a **mandatory** rollback snapshot, created and then marked (`rollback_snapshot_create_for_post_write()` / `rollback_snapshot_mark_post_write()`, `rollback_snapshot_mark_from_write_error()` on failure), ignoring the `backup` param every single-post writer consults, because the one site-wide write does not get an off switch for its recovery data; an unmarked snapshot would be permanently unrestorable, since `rollback_snapshot_restore()` refuses an empty `after.checksum`. A **round-trip fidelity gate** (`block_round_trip_drift()`) refuses any page whose unmodified `parse_blocks_for_write()` → `serialize_blocks()` is not byte-identical, enforced in dry-run too. `mode="apply"` now **requires `page_ids`** (`preset.apply_requires_page_ids`), so an apply writes the pages a plan reported instead of re-resolving the whole site at apply time. A partial apply returns **`ok: false`** (`preset.reassign_partial_failure`, HTTP 207) with the whole summary — including the snapshot id of every page that was written — in `error.data`. The written count is read from the per-page evidence (`preset_reassign_written_page_ids()`, surfaced as `summary.pages_written`) rather than derived from `pages_modified`, which is incremented before four of the five failure modes and so supports no arithmetic that recovers the total. The `global_layout_wrapper_drift()` call is deliberately kept ahead of the guarded write rather than delegated to the guard's opt-in `$check_global_layout_drift`, so a page that will be refused never leaves a snapshot row behind. Covered by `tests/test-preset-reassign-write-safety.php`: behavioral tests over the pure helpers, one request-level test that the apply refusal reaches the caller as an envelope before the registry read, and structural guards read from the real method source via Reflection — the same split `tests/test-parse-blocks-for-write-coverage.php` uses, because `parse_blocks()` and Divi's `et_get_option()` are both deliberately unshimmed and this suite does not fake either (#188). | #11, #188 |
| `plugins/diviops-agent/includes/trait-validate.php` | `validate_blocks()` (the standalone validation tool) had drifted from the #11 pattern like `trait-theme-builder.php`'s `normalize_and_validate_divi_markup_before_write()` and parsed via bare `parse_blocks()`, so a page carrying a `divi/global-layout` wrapper validated against a silently materialized tree instead of the wrapper itself outside a genuine REST dispatch. Fixed to `parse_blocks_for_write()`; verified live both by content and by `page_id` against page 900390 (#99). | #99 |
| `plugins/diviops-agent/includes/trait-library.php` | Adds `library_delete()` — the `library` domain had list/get/save but no removal path, the only content-holding domain without one (removal meant wp-admin or a raw `wp post delete`). Mirrors the sibling `page_trash`: soft-trash by default (reversible), opt-in `force` for permanent `wp_delete_post`, dry-run planning, and idempotent no-op semantics on an already-trashed item (returns `already_trashed`). Guards on post existence AND `post_type === 'et_pb_layout'` so it cannot trash a non-library post that shares the id space, then a per-object `delete_post` capability check. Trash is safe for the domain's re-save contract because `library_existing_id_by_title()` queries `post_status => 'any'`, which excludes trash, so a trashed item never blocks re-saving its title. No Divi-cache invalidation, matching the sibling `library_save`. | #26 |
| `plugins/diviops-agent/includes/trait-variable.php` | Adds `variable_update()` — the `variable` domain had create/delete/create-fluid-system/scan-orphans/used-on-page but no update, so changing a token's value meant delete-then-recreate, which mints a fresh id unless the caller explicitly re-supplies the old one, and even then overwrites unconditionally with no not-found/partial-merge contract. `variable_update()` looks up the existing record by id (auto-detecting the `colors` vs `et_divi_global_variables` bucket from the `gcid-`/`gvid-` prefix, same as `variable_delete()`), rejects an unknown id with `not_found`, and merges only the caller-supplied fields (label/value/status) via the new pure helper `build_updated_variable_record()`. That helper never receives `id` or `type` in its override set, so both are structurally immutable through this endpoint — the property existing `$variable({...})$` page references depend on, since the token embeds the id, not the value. `value` validation mirrors `variable_create()`'s per-type rules, reusing `build_gradient_variable_value()` for `type=gradients`. Does not regenerate a fluid `clamp()` from min/max/targets — that shorthand stays create-only. | #25 |
| `plugins/diviops-agent/includes/trait-menu.php` | Adds the four menu-CRUD handlers the `menu` domain was missing — it shipped create/get/list/item-add-page/item-add-custom/location-assign but no removal, reorder, or location-clear path. `menu_delete()` permanently deletes a menu (nav menus have no trash, so no `force`), capturing the theme locations assigned to it before `wp_delete_nav_menu()` frees them and reporting them in `freed_locations`. `menu_item_remove()` removes one item: by default it re-parents the target's direct children to the target's own parent (via the `_menu_item_menu_item_parent` post meta) so the tree stays connected, then deletes the target; `cascade=true` removes the target and every descendant, walked within the menu. `menu_item_reorder()` renumbers `menu_order` 1..N across one level, rejecting any `order` that is not an exact duplicate-free permutation of the ids whose parent equals `parent`. `menu_location_unassign()` mirrors `menu_location_assign()` in reverse — validates the location is registered, no-ops idempotently when it is not assigned, and otherwise unsets it from `nav_menu_locations` via `set_theme_mod()`. All four follow the existing menu patterns (the `menu_can_manage`/`menu_forbidden` gate first, `menu_object_by_id`/`menu_not_found`, inline dry-run plans, `menu_readback` success readback). | #30 |
| `diviops-server/src/index.ts` | Adds the `diviops_variable_update` tool, mirroring `diviops_variable_create`'s per-type `value`/`gradient` input shape and `diviops_global_font_update`'s partial-update framing, calling `POST /variable/update` (#25). Adds the `diviops_library_delete` tool, mirroring `diviops_page_trash`'s `force`/`dry_run`/idempotent shape, calling `POST /library/delete/{id}` (#26). Adds an optional `post_type` input (default `page`) to `diviops_page_create`, forwarded to `POST /page/create` (#31). Adds the four menu-CRUD tools — `diviops_menu_delete` (mirrors `diviops_menu_get`'s id shape, POST + dry_run, no force since nav menus have no trash), `diviops_menu_item_remove` (menu_id/item_id/cascade/dry_run), `diviops_menu_item_reorder` (menu_id/order[]/parent/dry_run), and `diviops_menu_location_unassign` (location/dry_run, the mirror of `diviops_menu_location_assign`) — and refreshes the `diviops_menu_item_add_page` / `_add_custom` / `_location_assign` descriptions that previously advertised no delete/reorder/unassign path (#30). Adds the `diviops_page_block_insert` tool, mirroring `diviops_tb_layout_block_insert`'s selector/path/position/backup shape, calling `POST /page/block-insert/{id}` (#32). Adds the four native-revision tools — `diviops_revision_list` (post `id` → newest-first metadata), `diviops_revision_get` (`revision_id` → raw content), `diviops_revision_diff` (`from` required, optional `to`, else vs current), and `diviops_revision_restore` (`revision_id`/`dry_run`, mirroring `diviops_rollback_snapshot_restore`'s POST+dry_run shape) — calling `/revision/*`; inspected-only when originally added, since the server did not build from this repo at the time (fixed by #41) (#34). Adds the four media tools — `diviops_media_upload` (exactly one of `url` or `data_base64`+`filename`, plus `attach_to`/`title`/`alt`/`caption`/`dry_run`), `diviops_media_get` (`attachment_id`), `diviops_media_list` (`page`/`per_page`/`mime`/`search`), and `diviops_media_set_featured_image` (exactly one of `attachment_id` or `url`, `dry_run`; idempotent only on the `attachment_id` path, since the `url` path uploads a fresh attachment on every call) — calling `POST /media/upload`, `GET /media/get/{id}`, `GET /media/list`, and `POST /media/set-featured-image` (#28). Adds the `diviops_theme_options_update` tool for the pre-existing `POST /theme-options` route, which had no MCP tool. The route predates the envelope convention (`WP_Error`/`rest_ensure_response`, not `{ok,data}`) — no client-side translation was needed because `wp.requestEnveloped`'s existing legacy-`WP_Error` and 2xx-non-enveloped-wrap branches already normalize it. `dry_run` is computed entirely client-side (the route has no `dry_run` support): reads current values via the `/schema/settings` route diviops_schema_get_settings also calls, then diffs against the requested `options` restricted to the write route's 9-key allowlist (`heading_font`, `body_font`, `accent_color`, `secondary_accent_color`, `font_color`, `header_color`, `link_color`, `heading_font_size`, `body_font_size`) — a key list that is NOT identical to `filter_public_theme_options()`'s 10-key read allowlist (which also includes `body_header_size`); unrecognized keys surface as a dry_run warning rather than a change, mirroring the write route's own silent-drop behavior (#29). Adds the three dynamic-content tools — `diviops_dynamic_content_list` (optional `post_id`/`context`, calls `GET /dynamic-content/list`), `diviops_dynamic_content_build` (`name`+optional `settings`/`type`, calls `POST /dynamic-content/build`), and `diviops_dynamic_content_validate` (exactly one of `name`+`settings` or a raw `value` token, calls `POST /dynamic-content/validate`) — mirroring `diviops_validate_blocks`' "the request succeeds even when the finding is invalid" envelope convention (#36). Adds the `diviops_page_duplicate` tool, mirroring `diviops_page_create`'s created-post response shape and `diviops_page_block_insert`'s `page_id`-in-path/dry_run shape, calling `POST /page/duplicate/{id}` (#35). Corrects `diviops_schema_get_module`'s description, which described `dump_all`'s `schema_version` as "a hash over the canonical *PresetAttrsMap.php files" with no scope at all — the phrasing that let the plugin docblock's narrower claim propagate unchallenged. It now names the hashed root (the Divi theme's `Packages/`, both `Module/Options/` and `ModuleLibrary/`) and states that a third-party module plugin update does not move the hash. Description text only; no tool, schema, or route behavior changes (#122). All three registration wrappers (`registerPluginTool`, `registerLocalTool`, `registerProTool`) now route their handler through a shared `withRequestContext()` that establishes the ambient `RequestContext` (`src/request-context.ts`) for the call, so the SDK's per-request `AbortSignal` reaches the transport instead of being dropped at the wrapper — upstream's own wrappers drop it identically, so this is a deliberate step past upstream, not a regression repair. The wrapper passes the SDK's arguments through verbatim and reads `extra` from the LAST position rather than a fixed second position, because a tool declared without an `inputSchema` is called `handler(extra)` and one with an `inputSchema` is called `handler(args, extra)`; a fixed-position read would silently disable cancellation for every no-`inputSchema` tool (`diviops_schema_list_modules` among them). `toolTransportAbortable()` derives the per-tool policy from the registration metadata the tool already declares (`annotations.readOnlyHint` / `annotations.idempotentHint` / `_meta.idempotent`), so the policy cannot drift from what `tools/list` advertises. No handler signature changed and no `wp.request`/`wp.requestEnveloped` call site was edited (#134). Adds `svg_capability_required` (403) to `diviops_media_upload`'s description, which enumerates the error codes the route can return and would otherwise describe the sanitizer as the only way an SVG upload can be refused (#73). Updates `diviops_preset_reassign`'s description and its `page_ids` / `mode` field descriptions for the write-safety contract: `page_ids` is required when `mode="apply"`, applied pages carry a `snapshot_id`, the per-page refusal codes are named, and a partial apply is `ok: false` rather than a success (#188). | #25, #26, #31, #30, #32, #34, #28, #29, #36, #35, #122, #134, #73, #188 |
| `diviops-server/src/wp-client.ts` | `request()` and `requestEnveloped()` resolve the signal they hand `fetch` through a new `effectiveRequestSignal()` instead of passing the caller's `signal` straight through. An explicit caller signal still wins outright (the meta tools thread theirs directly). Absent one, the ambient MCP request signal from `src/request-context.ts` applies — but reaches `fetch` only when the invoking tool is transport-abortable. A mutating tool's in-flight request is deliberately left un-abortable: WordPress offers no rollback for a write cut off partway, so a half-applied `module_update` is a worse outcome than a write that completes after the client stopped listening. Both classes refuse to dispatch at all when the governing signal has already aborted, which costs nothing because no bytes have been sent. `testConnection()`'s abort rethrow now also consults the ambient signal, so a cancellation is not reported as a "connection failed" the caller would read as a site problem (#134) | #134 |
| `diviops-server/src/envelope.ts` | `wrapResponse()` rethrows instead of converting to an envelope when the ambient request signal has aborted. Without this, a cancelled call would be reported to the client as a soft `{ ok: false, error: { code: 'wp_error' } }` connection failure it could sensibly retry. The discriminator is the signal's own `aborted` flag rather than the thrown error's shape, because an aborted `fetch` rejects with whatever value was passed as the abort reason, up to and including a bare string with no recognizable name (#134) | #134 |
| `diviops-server/src/schema-route.ts`, `diviops-server/src/__tests__/schema-route.test.ts` | Fixes `schemaModuleRoute()`, which applied `encodeURIComponent()` to the whole module name and so turned a namespace separator into `%2F` — a shape the plugin's route pattern (`/schema/module/(?P<name>[a-zA-Z0-9_/-]+)`) does not match. Every module outside the `divi/` namespace was therefore unreachable through `diviops_schema_get_module`: 109 of 194 on the reference site, all 108 DiviFlash (`difl/*`) plus `d5bgo/*`. `divi/*` masked the bug because its prefix is stripped before encoding, leaving no slash to mangle — which is why the fork's own native-introspection work (#42 → #57, #66), being PHP-side and `divi/*`-focused, never surfaced it. The plugin was never at fault. Now encodes each path segment individually so the separator survives while genuinely unsafe characters are still escaped. Adds the file's first test coverage (`schema-route.test.ts`, 6 cases spanning all three namespace shapes on the reference site plus unsafe-character handling) (#120) | #120 |
| `README.md` | Taken over as the maintained fork's README (#49). Reframes the intro as `rubicon/diviops` with explicit credit to `oaris-dev/diviops` and a maintained-fork note (fork capabilities + the drop-in guarantee that keeps Pro and the MCP server working). Clarifies the repository's dual-licensing in the License section and badge — MIT for the server/skills/templates/docs/tests, GPL-2.0-or-later for the WP plugins — replacing the prior "MIT" oversimplification (no code license change; the split is upstream's own, per `LICENSE`). Adds a Pro-compatibility + clean-room-skills note to the Free/Pro section (#49, #50). Documents the media domain (`media_*`: upload from URL/base64, get, list, set-featured-image, update-meta) and the Safe SVG sideload-sanitizer requirement (SVG uploads fail closed with `svg_sanitizer_required` without a verified Safe SVG binding on `wp_handle_sideload_prefilter`), plus deployment hardening notes and a cross-reference to #73. That cross-reference (which described the capability gate as tracked separately) is replaced by the setting's own operator documentation: what `DIVIOPS_SVG_UPLOAD_CAPABILITY` does, that the constant beats the env var, that the default `upload_files` changes nothing, that `do_not_allow` disables SVG uploads outright, and that the capability layers on top of the Safe SVG requirement rather than replacing it (#73). Adds `revision_*` and `menu_*` rows to the Tools-at-a-glance table (previously undocumented at this altitude) and corrects the always-on tool count from a stale 74 to the verified 109 (`registerPluginTool`/`registerLocalTool` call count in `diviops-server/src/index.ts`). Corrects the Free/Pro table's Advanced-attributes row, which claimed Pro-only, to reflect that this fork's own clean-room `advanced-attributes.md` (#62) already ships in the Free skill directory; adds a versioning-process paragraph (Conventional Commits + release-please) to the Free/Pro intro (#76). Corrects the always-on tool count again, from a stale 109 to the verified 114 (`registerPluginTool`/`registerLocalTool` call count in `diviops-server/src/index.ts`), and adds the previously-undocumented conditional-Pro-tool figure (30, from `registerProTool` call count) inline in the Tools-at-a-glance intro sentence (#90). Corrects the always-on tool count again, from a stale 114 to the verified 115, for the new `diviops_page_duplicate` tool (#35). | #49, #76, #90, #35, #73 |
| `diviops-server/README.md` | Adds `media_*` and `revision_*` rows to the Tools-at-a-glance table, corrects the always-on tool count from a stale 94 to the verified 109, and adds a Media-domain-and-SVG-uploads summary paragraph cross-referencing the main README's fuller write-up (#76). Corrects the always-on tool count again, from a stale 109 to the verified 114. Repoints the 11 of 14 dangling `../docs/*.md` links (a directory that has never existed in this repo's history) that map onto existing `SETUP.md` content (`setup-guide.md` → `../SETUP.md`, `wp-cli-security.md` → `../SETUP.md#wp-cli-security`, `troubleshooting.md` → `../SETUP.md#troubleshooting`) and rewrites the surrounding prose for the remaining 3 (`server-reference.md`, `safety-patterns.md`, `idempotency-audit.md`, none of which was ever written) so the README no longer promises documents that don't exist; the per-tool reference gap is tracked as a follow-up (#93) (#90). Adds the SVG-capability-gate sentence to its own Media-domain-and-SVG-uploads summary paragraph, so the server README does not describe the sanitizer requirement as the only SVG gate (#73). Corrects the always-on tool count again, from a stale 114 to the verified 115, for the new `diviops_page_duplicate` tool (#35). | #76, #90, #35, #73 |
| `skills/divi-5-builder/references/module-formats.md` | First fork-owned edit to this file (previously pure upstream sync). Adds the `divi/cta` block to the "Generated path index" (previously absent entirely) and three Exceptions Quick Reference rows: CTA's `title` is a plain string, unlike Blurb's object shape — writing Blurb's shape instead throws a PHP fatal (`MultiViewUtils.php:1253`) on render, not a silent no-op; `divi/accordion`'s own `title`/`content`/etc. attrs (already present in the pre-existing generated index) are never rendered, with or without `divi/accordion-item` children — every item must be a child block, and the first child opens by default with no attribute controlling it (neither module declares `module.advanced.open`, unlike Toggle); `divi/toggle`'s `module.advanced.open` controls initial open/closed state. The `divi/cta` block was authored from `schema/module/divi/cta` introspection, matching this file's own established Tier-3 generation convention for the other 28 modules — then cross-checked against `CTAPresetAttrsMap.php` (Divi's actual preset-registration source, the canonical dot-path source per the #63 issue-comment correction) and found fully corroborated at the group level, though shallower than that source's full leaf-path depth. **Known gap, not fixed here**: the entire pre-existing 28-module "Generated path index" (unmodified by this PR) is sourced the same schema-dump way, not from `*PresetAttrsMap.php` — re-deriving it at `advanced-attributes.md`'s full leaf-path depth is a larger, separately-scoped effort. Also notes in the doc itself that `diviops-server/scripts/regen-module-formats.mjs` and `diviops-server/CONTRIBUTING.md`, both referenced by this file's own generated-index header, don't exist anywhere in the repo — the generation tooling was never committed (#63) | #63 |
| `diviops-server/scripts/smoke.mjs` | Fork-authored, absent upstream. A credential-free MCP smoke gate that spawns the built `dist/index.js`, completes a real stdio handshake against a closed port (`127.0.0.1:9`), and asserts the always-on surface: 115 tools, the handshake version matching `package.json`, a representative tool from each capability domain present, no duplicate names, and the 30 Pro tools ABSENT (they register only against a live handshake). No source change was needed — `requireCredentials()` is a presence check only and `main()` treats a failed handshake as non-fatal by design, so dummy values yield a complete `tools/list`. It gates `npm publish`, so it is written to fail loudly rather than pass vacuously; mutation-verified — a wrong expected count and a non-existent expected tool each fail it (#163). | #163 |
| `.github/workflows/publish.yaml` | Fork-authored, absent upstream (upstream ships no CI at all). Publishes `@rubicontv/diviops-mcp` on a published GitHub Release, gated on `npm run smoke`, with npm provenance (`id-token: write`). **Guarded on the `mcp-server-v` tag prefix**: this repo cuts two release trains, so an unguarded `on: release: [published]` — the shape of the `rubicon/forgejo-mcp` template it was modeled on, which is single-package — would run `npm publish` on plugin releases too. Uses a bootstrap `NPM_TOKEN`; trusted publishing (OIDC) cannot perform a package's first publish, so the token is phase 1 and its removal is tracked separately (#163). | #163 |
| `CONTRIBUTING.md`, `SUPPORT.md`, `.github/ISSUE_TEMPLATE/bug_report.md` | Previously pure upstream sync; now diverged solely by the npm package rename from `@diviops/mcp-server` to `@rubicontv/diviops-mcp`. Upstream solely owns `@diviops/mcp-server` (`npm owner ls` → `oaris <tools@oaris.de>`), so this fork can never publish under that name; the package name is NOT one of the four frozen identifiers, so renaming it does not affect Pro attachment or MCP tool resolution. The `bin` name stays `diviops-mcp` (#163). | #163 |
| `diviops-server/src/cross-env-preflight/README.md`, `diviops-server/scripts/copy-vendored-cross-env-preflight.mjs` | **Provenance — the `@diviops/mcp-server` references in these two files must NEVER be renamed.** They record that the vendored compiled files came verbatim from upstream's published `@diviops/mcp-server@1.5.39` tarball. Renaming them to this fork's own package name would falsify the provenance record and break the documented re-vendoring procedure (#163). | #41, #163 |
| `plugins/diviops-agent/includes/trait-rollback.php` | Bounds the snapshot store, which upstream left unbounded. Snapshots are one `wp_options` row each holding the target's full prior `post_content`, and nothing ever deleted them: the 7-day `expires_at` they carry is read only by an admin badge (`expired` never gates `rollback_snapshot_restore()`), and the plugin registers no WP-Cron anywhere. Adds `rollback_snapshot_retention_limit()` (500), `rollback_snapshot_retention_batch()` (50), and `rollback_snapshot_enforce_retention()`, called from `rollback_snapshot_create_for_post_write()` after the row is safely stored. The bound is a **count**, not a time: `rollback_snapshot_managed_inventory()` caps at 1000 records, and a busy site can produce more than that inside the 7-day window, so only a count cap keeps the ceiling unreachable. Eviction is best-effort (ignores per-row failures, never returns an error, so reclaiming space cannot fail an otherwise-good content write), bounded per write (an upgrading site can arrive with a large backlog; it drains across writes rather than risking a timeout), and only ever deletes rows whose name parses as a snapshot id. Separately, the inventory's window is now `ORDER BY option_id DESC` instead of `option_name ASC` — it previously selected oldest-first, so crossing the ceiling dropped the *newest* snapshots, exactly the ones a recovery needs; `option_id` is the database's own monotonic sequence, which the method already reports as `storage_sequence`, whereas `option_name` embeds a creation-time `gmdate()` stamp that misorders under clock skew. The `:645` presentation `usort` and the `complete`/`truncated` contract Pro reads are unchanged | #187 |
| `plugins/diviops-agent/README.md` | Removes the SCF and CPT capability claims. Upstream's copy advertised "SCF setup + management" and "SCF field group inspection" among the plugin's own handshake capabilities, but the plugin contains no SCF code: no `trait-scf.php`, no `/diviops/v1/scf/*` route, no `scf_*` capability key. All six `diviops_scf_*` tools are `registerLocalTool` wrappers in `diviops-server/src/index.ts` that shell out over WP-CLI, so they are never handshake-gated and register even against a site with no SCF installed. Replaces those claims with a "What this plugin does not implement" section stating the behavioural difference, and corrects the stale figures (98 capability keys to the real 123, 91 always-on tools to the real 115). Repoints the five links to `../../../docs/*.md`, a directory that has never existed in this repo's history, at a depth that escaped the repo root even for targets that do exist: `setup-guide.md` and `troubleshooting.md` onto `../../SETUP.md`, `server-reference.md` onto the generated per-tool reference in `../../diviops-server/README.md`, matching the treatment #90 gave the server README's copies of the same dangling links (#168). | #168 |
| `plugins/diviops-agent/README.md`, `plugins/diviops-agent/readme.txt`, `plugins/diviops-agent/diviops-agent.php` (Getting Started block) | Repoints every plugin-facing reference from `@diviops/mcp-server` to this fork's own `@rubicontv/diviops-mcp`. These deliberately rode a LATER release train than the server-side rename (#163): until `@rubicontv/diviops-mcp` actually existed on npm, changing them would have pointed users — and a WordPress.org third-party-service disclosure — at a package that 404s. Unblocked once `@rubicontv/diviops-mcp@1.7.0` published with signed provenance. The old name is upstream's still-published package, solely owned by `oaris`, so the previous install commands installed upstream's server and none of this fork's work — a doc that worked while doing the wrong thing. `readme.txt`'s external-services section is a WordPress.org disclosure requirement and must name the package users actually download. The `bin` name stays `diviops-mcp`, so only the `--package` argument changes (#169). | #163, #169 |
| `LICENSE`, `diviops-server/LICENSE`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `plugins/diviops-agent/diviops-agent.php`, `plugins/diviops-design-library/diviops-design-library.php`, `skills/diviops/SKILL.md`, `skills/divi-5-builder/SKILL.md` | Asserts this fork's own copyright and takes over authorship metadata. Both `LICENSE` files gain a `Copyright (c) 2026 Dax Davis` line beside the existing `Copyright (c) 2026 oaris.de`, with a sentence stating that oaris.de holds copyright in the original forked work and Dax Davis in this fork's contributions. Upstream's notice is retained in full, as both MIT and GPL-2.0-or-later require, and no licensing terms change: the MIT/GPL split is upstream's own, and the GPL side is required by Divi and WordPress independent of the fork. The authorship fields across the shipped artifacts (plugin `Author`/`Author URI` headers, the Claude Code plugin and marketplace manifests, and both skill frontmatters) named upstream while the fork sets the roadmap, README, skills, and release process, so they now name the maintainer. `plugins/diviops-design-library/diviops-design-library.php` also had a `Plugin URI` still pointing at `oaris-dev/diviops`. `diviops-server/package.json` already carried the correct identity and is unchanged. Per-file copyright headers were deliberately NOT added: no file in the tree carries one, and stamping only the three fork-authored traits would imply the other eighteen changed files are wholly upstream's, which is false (`trait-core.php` alone is 668 changed lines of 2440). Git history remains the per-file authorship record. | #231 |

### Deliberately unchanged: `post_uses_divi()` / `content_uses_divi()`

These decide "is this Divi content" on the literal substring `<!-- wp:divi/`, and issue
#2's inventory lists them. They are intentionally left alone. Loosening them to accept
any namespace would misclassify ordinary Gutenberg pages as Divi content, because
unrelated third-party blocks (`gravityforms/*`, `pdfemb/*`, `tec/*`) are registered
alongside the Divi ones. The check is also not actually failing: across all 108 posts
on the reference install that carry `difl/*` or `d5bgo/*` blocks, every one also
contains a `<!-- wp:divi/` marker and every one opens with a `divi/` block, because
third-party modules nest inside Divi sections rather than replacing them.

## Upstream tracking

```bash
git remote -v
# origin    git@github.com:rubicon/diviops.git
# upstream  https://github.com/oaris-dev/diviops.git

git fetch upstream
git merge upstream/main        # through the normal issue, branch, PR flow
```

Files that originated upstream (`README.md`, the other repo docs, and everything
under `plugins/`, `diviops-server/`, and `skills/`) may be edited or taken over as
needed under the Maintained-fork posture above — we are not minimizing divergence
from upstream. Record intentional changes in the divergence tables above so a future
upstream cherry-pick stays predictable. Fork operating instructions still belong in
this file or `CLAUDE.md`, never inline in a doc that also serves end users.

## Deliberately out of scope

### Fixed: the `divi/global-layout` index divergence

`page_get_layout` walks the parsed block tree and relabels a `global-layout`
wrapper as the `blockName` it resolves to, counting it as `section:1`. The
raw-string scanners and parsed-tree walkers behind `module_update`, `module_get`
(via `find_block()`), the section operations (via `find_all_sections()`), and
`module_lock` / `module_unlock` / `module_clone` (via `walk_and_mutate()`) used to
count the wrapper literally as `global-layout:1` instead, so every real block after
it landed one `auto_index` position off from what the read path reported. Verified
live against reference page 900390.

Fixed in #13 by resolving the wrapper's counted type from its own
`attrs.blockName` — the wrapper carries the type it resolves to right in its own
opening comment (`{"globalModule":"900296","blockName":"divi/section",...}`), so
reading that attr is enough to agree with the read path. `counted_block_name()` /
`counted_block_identifier()` in trait-core.php are the shared resolution; every
site above routes through them. This is a string/attrs-level fix with no
`parse_blocks()` / `serialize_blocks()` round-trip and no new write blast-radius,
which is what the original scoping had assumed a fix would require.

`module_move`'s parser-backed collector (`collect_parser_move_blocks()`) and
`module_get`'s parser-backed fallback (`collect_readable_divi_blocks()`, reached
only after the raw scanner already fails on malformed markup) were not brought
into #13's fix and still counted a wrapper literally. Fixed in #14 by routing
both through the same `counted_block_identifier()` resolution, so the divergence
is now closed for the parser-backed fallback paths the same way #13 closed it
for the raw-scanner and parsed-tree paths. The real-world exposure was narrow
either way: both fallbacks are reached only when the raw `find_block()` scanner
already returns a `parse_error` on malformed markup.

### Fixed: `find_all_sections()` missing the self-closing check

`find_all_sections()` did not check `is_self_closing` before starting its
nesting depth-scan, unlike `find_block()`, which already did. #13's fix gave
this pre-existing gap a new way to surface: a **self-closing**
`divi/global-layout` wrapper that resolves to a `*/section` name passes the
namespace-agnostic section filter, but since it has no closer of its own the
unguarded depth-scan either consumed an unrelated later closer or found none.
More generally, `find_all_sections()`'s outer loop only advances past
whichever opener it just processed, not past that opener's whole matched
span, so it revisits every descendant opener again as its own top-level
candidate. When a revisited self-closing same-name opener reached the
unguarded depth-scan, it consumed the enclosing (or a later) section's real
closer and reported a bogus match whose bounds overlapped a real one.
`find_block()` was unaffected, since it already checked `is_self_closing`.

Fixed in #12 by giving `find_all_sections()` the same `is_self_closing` check
`find_block()` already had: a self-closing section opener, wrapper or not, is
now a complete one-comment span, matched directly rather than routed through
the depth-scan.

### Fixed: the `module_lock` / `module_unlock` / `module_clone` write hazard

Six write paths round-trip a page's content through `parse_blocks()`, mutate the
parsed tree, and write it back with `serialize_blocks()`: `module_lock`,
`module_unlock`, and `module_clone` (shared via `load_post_for_module_op()` /
`save_mutated_blocks()`), `module_move`'s parser fallback
(`move_block_with_parser()`), `tb_layout_block_insert()`, and `preset_reassign()`.
On a page carrying a `divi/global-layout` wrapper, re-serializing the whole tree
risked materializing the wrapper's resolved content into the page: Divi 5's own
block parser expands that wrapper unless a skip condition holds, and one of those
conditions is `_is_rest_update_request()` — `Conditions::is_rest_api_request()`
plus a `$_SERVER['REQUEST_METHOD']` check for POST/PUT/PATCH (confirmed by reading
the real method on the reference Divi install during #99's investigation, which
corrected an earlier, inaccurate description here claiming a fragile
`$_SERVER['REQUEST_URI']` string match) — which reliably holds for genuine REST
dispatch but fails open for `wp eval`/CLI invocation, a data-integrity hazard
independent of the numbering question #13 fixed. Verified live against reference
page 900390: a bare `wp eval` parse collapsed its one wrapper to zero.

Fixed in #11 with two layers. Layer 1 prevents the expansion outright:
`parse_blocks_for_write()` (trait-core.php) routes every one of the six sites'
parse through Divi's own
`ET\Builder\FrontEnd\BlockParser\BlockParserUtils::parse_blocks_with_layout_context( $content, 'saving_content' )`,
which sets an unconditional skip signal independent of request shape, confirmed
by reading `BlockParser::parse()` on the reference Divi install. It falls back to plain
`parse_blocks()` via `class_exists()`/`method_exists()` when that Divi class is
unavailable. This also covers `tb_layout_block_insert()`'s caller-supplied
INSERTION content (`parse_divi_blocks_for_insert()`), not only the stored layout
being inserted into — adversarial review found that a wrapper arriving in the
inserted content itself would otherwise be expanded before it ever reached the
tree, a loss the drift-guard cannot catch because its baseline never contained
that wrapper to begin with.

Layer 2 is the backstop for when Layer 1 could not apply. `global_layout_wrapper_
identities()` is a JSON-aware scan (same discipline as `block_opener_is_self_
closing()`) that reads each wrapper's `globalModule` id in document order, falling
back to a sentinel token for a wrapper with no readable id so it is still tracked
by count. `global_layout_wrapper_drift()` compares the two sides' id multisets —
identity-aware, not count-only, because a wrapper swapped for a different one
(layout A replaced by layout B) is drift even though the overall wrapper count is
unchanged; Divi's non-recursive expansion can produce exactly that shape for
nested or chained global layouts, and a count-only comparison would miss it.
Either side's scan returning `null` — an opener anywhere in the document that
can't be resolved to its own comment terminator, or a wrapper's own attrs failing
to decode — is itself treated as drift: a malformed prefix ahead of a genuinely
removed wrapper would otherwise truncate both scans to the same wrong count and
mask the loss, so an unreadable scan refuses the write rather than approving it
by default. `update_post_content_with_integrity_guard()` carries this as an
opt-in parameter so it does not also gate the raw-content writers
(`page_update_content`, `tb_layout_update`) that share the same function and are
legitimately allowed to drop a wrapper on purpose; `preset_reassign()` bypasses
that shared function for its own batch write and calls the drift check directly.

With Layer 1 active (the common case), no expansion occurs and Layer 2 never
fires — full capability is preserved everywhere, including `wp eval`. Layer 2 only
matters when Layer 1 could not apply, refusing that one write rather than
corrupting it.

`global_layout_wrapper_drift()` is unit-tested
(`tests/test-global-layout-write-guard.php`): a lost wrapper, a benign reserialize
with the same id, no wrapper on either side, one of two wrappers lost, an
id-swap that a count-only comparison would miss, an unterminated opener ahead of
a removed wrapper (fail-closed catches what a partial count would mask), identical
malformed markup on both sides (a documented deliberate over-refusal — an
unreliable scan cannot prove nothing was lost), and a JSON-embedded decoy
substring that must not be miscounted, plus in-file mutation checks proving each
of those three properties is load-bearing rather than incidental: the
identity-aware comparison (a naive same-count check would call the id-swap
fixture "no drift"), fail-closed (a naive "unscannable equals no drift" check
would allow the malformed-ahead-of-a-removed-wrapper fixture), and the
JSON-aware scan (a naive `substr_count` comparison would misread the decoy
fixture as a lost wrapper). Layer 1 was verified live and read-only against
reference page 900390: `parse_blocks_with_layout_context()` held the wrapper
count at 1 where a bare `parse_blocks()` round trip collapsed it to 0. The full
write path on all six sites (an actual `module_lock`/`clone`/etc. call against a
live database) is not exercised by either the unit tests or that read-only check
and remains gated on the maintainer's own live verification before merge.

`update_post_content_with_integrity_guard()`'s refusal carries a distinct error
code for each of the two reasons the guard can refuse a write (#23): the
comparison is a fail-closed `null` scan on either side (a malformed/unterminated
block comment makes the content unverifiable, whether or not a wrapper is even
present) versus an actual `globalModule` id going missing between the two
sides. `global_layout_write_refusal_reason()` returns `null` / `'identity_lost'`
/ `'scan_unreliable'` so the two cases surface as `{namespace}.global_layout_
drift` (the original wrapper-materialization message, accurate only for an
actual identity loss) and the new `{namespace}.global_layout_scan_unreliable`
(the content could not be verified, so the guard refused rather than risk a
masked loss — nothing was necessarily lost). Before #23, both cases produced the
wrapper-materialization message, which was false on a page with no wrapper at
all whose markup happened to be malformed elsewhere.
`global_layout_wrapper_drift()` is now a thin bool wrapper around the reason
function, kept for `preset_reassign()`'s own direct call, which only needs the
fail/pass verdict.

Third-party targeting does not depend on either of the above: third-party counters
are per block name and are unaffected by the wrapper.

## Licensing

The WordPress plugins are GPL-2.0-or-later, stated in the upstream `LICENSE`, in each
plugin header, and in `readme.txt`. Forking and modifying them is expressly
permitted. The MCP server, skills, templates, and docs in this repository are MIT.
Upstream's copyright notice is preserved exactly as upstream wrote it, in both `LICENSE`
files, as both licenses require. Alongside it this fork asserts its own: oaris.de holds
copyright in the original work, Dax Davis in the contributions made here (#231). Adding
that line changes no licensing terms and removes nothing.

## Contributing back

The namespace-agnostic change is a generic fix that benefits every DiviOps user, not
just this fork. The intent is to offer it upstream as a pull request once it is
proven here. An upstream PR is gated on the maintainer's explicit per-PR approval and
follows the Outbound PR Authorship Standard.

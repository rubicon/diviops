# MCP Tool Reference

Curated Divi-builder authoring subset. The full DiviOps server currently registers 145 tools: 115 always-on tools (104 plugin-routed + 11 local) and 30 conditional Pro tools. See [`diviops-server/README.md`](../../../diviops-server/README.md) for the complete inventory. The six `diviops_scf_*` tools are documented in [scf-fields.md](scf-fields.md) (they are local WP-CLI wrappers, not plugin-routed). The separate `diviops-scf/` coverage slice named in the primer does not ship in this repository. Cross-cutting harness conventions (response envelope, capability handshake, `dry_run` plan shape, idempotency) live in the [diviops/](../../diviops/SKILL.md) primer skill.

## Response shape

Every `diviops_*` tool returns the standard envelope documented in the [diviops/](../../diviops/SKILL.md) primer (`{ ok, data?, error: { code, message, hint?, data? } }`). This includes WordPress REST framework parameter-validation failures on `/diviops/v1/*` routes: `rest_invalid_param` and `rest_missing_callback_param` are wrapped as canonical `invalid_input` with the original WP code preserved in `error.data.wp_error_code`. The slice-specific error codes the Divi-builder tools emit are listed below; the canonical codes (`not_found`, `invalid_input`, `validation_failed`, `conflict`, `capability_missing`, `forbidden`, `wp_error`, `divi_error`) come from the primer.

**Namespace-specific error codes** (introduced as namespaces adopt the envelope):

| code | HTTP | tool(s) | meaning |
|---|---|---|---|
| `meta_wp_cli.not_configured` | 400 | `diviops_meta_wp_cli` | WP_PATH / WP_CLI_CMD env var missing — wp-cli runner not initialized |
| `meta_wp_cli.command_failed` | 400 | `diviops_meta_wp_cli` | wp-cli failed in one of four ways. Carries `error.data = { exit_code: number \| null, stdout: string, stderr: string }`. Disambiguate by `error.message` prefix: **(a) Numeric `exit_code`** — wp-cli ran and exited non-zero. `stdout`/`stderr` are raw streams verbatim. **(b) `exit_code: null`, message starts with `"wp-cli command terminated:"`** — execFile launched the child but it was killed (timeout / signal). `stdout`/`stderr` carry whatever streamed before the kill; hint mentions raising the timeout. **(c) `exit_code: null`, message starts with `"wp-cli could not spawn:"`** — the OS refused to start the child (ENOENT / EACCES / EPERM); the child never ran. `stdout`/`stderr` are empty; hint mentions checking the binary path / executable bit. **(d) `exit_code: null`, message is the rejection reason** — pre-execution rejection by the allowlist / FS validator. Reason synthesized into `error.data.stderr` so callers see a uniform `{ exit_code, stdout, stderr }` shape across all four failure modes |
| `meta_flush_cache.unwritable` | 500 | `diviops_meta_flush_cache` | Cache root exists but the active backend cannot write to it (Divi `can_write_to_filesystem()` in native mode, `is_writable()` in fs_fallback) |
| `meta_flush_cache.fs_init_failed` | 500 | `diviops_meta_flush_cache` | `WP_Filesystem` could not be initialized (FTP/SSH-credentialed hosts without saved credentials). Native backend only |
| `variable.fluid_generation_failed` | 400 | `diviops_variable_create` | Algorithmic clamp() failure — input shape was valid but the math couldn't produce a clamp (e.g. degenerate viewport range, NaN-producing target values). Carries `error.data = { min, max, targets, reason }` so callers can branch on shape-vs-algorithm errors |
| `variable.fluid_system_generation_failed` | 400 | `diviops_variable_create_fluid_system` | Algorithmic scale-generation failure inside a typography/spacing/radius category (degenerate ratios, invalid steps, geometric-from-zero, etc.). Carries `error.data = { profile, categories, reason }` documenting which category triggered the failure |
| `variable.customizer_default_immutable` | 403 | `diviops_variable_delete`, `diviops_global_color_update`, `diviops_global_color_delete` | Target ID is one of Divi's customizer-bound color defaults (gcid-primary-color, gcid-secondary-color, gcid-heading-color, gcid-body-color, gcid-link-color); managed via WP Customizer theme options, not writable/deletable through these tools. Carries `error.data = { id, managed_by: 'wp_customizer' }`. Same identity across both namespaces — Divi 5.4+ unified gcid-* into the variable manager but kept the customizer-binding for the five legacy defaults; reuse the namespace-prefixed code so callers branch uniformly across both surfaces |
| `preset.bucket_mismatch` | 400 | `diviops_preset_reassign` | Cross-bucket swap rejected — `old_uuid` is a `module` preset and `new_uuid` is a `group` preset (or vice versa). Carries `error.data = { old_bucket, new_bucket }`. Module/group identity is disjoint by construction (different storage paths, different ref shapes); the preset registry has no concept of cross-bucket equivalence |
| `preset.scope_mismatch` | 400 | `diviops_preset_reassign` | Explicit `scope` param disagrees with `new_uuid`'s bucket — e.g. `scope='module'` with a group preset as `new_uuid`. Carries `error.data = { scope, new_bucket }`. Distinct from `preset.bucket_mismatch` because the constraint is caller-asserted, not registry-derived |
| `preset.too_many_pages` | 400 | `diviops_preset_reassign` | Explicit `page_ids` count exceeds the safety cap (REASSIGN_MAX_PAGES). Carries `error.data = { received, max_pages }`. Hint: chunk the request into smaller `page_ids` batches |
| `preset.apply_requires_page_ids` | 400 | `diviops_preset_reassign` | `mode="apply"` was called without `page_ids` (or with an empty array). Only dry-run scans the whole site; an apply writes exactly the pages you name, so it cannot touch pages the plan never showed. Carries `error.data = { field, mode }`. Hint: run the dry-run first and apply with the page ids it reported |
| `preset.reassign_partial_failure` | 207 | `diviops_preset_reassign` | An apply finished with at least one per-page failure. **This is `ok: false`** — a partial application is not a success, even though some pages were written. `error.data` is the complete response payload, so `error.data.summary.errors[]` holds the per-page reason and `error.data.summary.details[].snapshot_id` holds the rollback snapshot of every page that WAS written |
| `rollback_snapshot.run_restore_partial` | 207 | `diviops_rollback_snapshot_restore` | A run snapshot restore finished with at least one page refused. **This is `ok: false`** — a rollback that half worked is not a success, the same convention `preset.reassign_partial_failure` sets. `error.data.restored[]` and `error.data.refused[]` carry the per-page outcome; each refusal names `id`, a `reason`, and for write failures the integrity guard's `diagnostics` |
| `rollback_snapshot.run_restore_failed` | 409 | `diviops_rollback_snapshot_restore` | A run snapshot restore restored nothing at all. Same payload shape as the partial code above. Distinct status because no page changed, so there is nothing to reconcile — read `refused[]` and retry the pages you can |
| `conflict` (with `error.data`) | 409 | `diviops_preset_create` | Name collision in the same `(bucket, bucket_key)` — a preset with the same display name already exists under e.g. `group/divi/font` or `module/divi/blurb`. Carries `error.data = { existing_preset_id, bucket, bucket_key, name }` so callers can branch on reuse / rename / `preset_update`. Bucket coordinates are the natural addressing scope: a 'Hero Title' font preset and a 'Hero Title' button preset coexist (different buckets); two 'Hero Title' presets in the same bucket collide |
| `conflict` (with `error.data`) | 409 | `diviops_preset_delete` | Target preset is the registered default for its bucket. Carries `error.data = { preset_id, type, module, name, reason: 'is_default' }`. The `reason` discriminator field leaves room for future conflict reasons (e.g. `referenced_in_chain`) without reshaping. Hint: clear the pointer first via `diviops_preset_set_default` with `unset=true`, or pass `force=true` to delete and clear in one write |
| `conflict` (with `error.data`) | 409 | `diviops_canvas_duplicate` | Explicit `title` collides with an existing canvas under the same parent. Carries `error.data = { existing_canvas_id: number, parent_page_id: number \| null }` so callers can retrieve the conflicting canvas and decide whether to reuse, rename, or skip. Omit `title` to auto-suffix instead |
| `conflict` (with `error.data`) | 409 | `diviops_library_save` | Title collides with an existing library item under the same `(layout_type, scope)`. Carries `error.data = { existing_library_id: number, layout_type: string, scope: string }` so callers can retrieve the conflicting item and decide whether to reuse, rename, or delete-and-replace. Uniqueness scoped to `(layout_type, scope)` so a "Hero" section + "Hero" row coexist |
| `conflict` (with `error.data`) | 409 | `diviops_variable_delete` | Variable has live references in pages / TB layouts / library items / canvases / preset registry. Carries `error.data = { id: string, ref_count: number, locations: object[] }` so callers can audit before re-issuing with `force=true`; orphans will remain after a forced delete — run `diviops_variable_scan_orphans` to clean up |
| `conflict` (with `error.data`) | 409 | `diviops_global_color_delete` | Color has live references tracked by Divi's `usedInPosts` index (best-effort — the index is populated on VB save, so a color recently assigned via MCP may not yet be tracked). Carries `error.data = { id: string, ref_count: number, used_in_posts: object[] }`. The `used_in_posts` element shape is pass-through from Divi's index (NOT the discriminated-union `locations[]` shape `variable_delete` builds via parse_blocks) — different field name reflects the provenance distinction. Pass `force: true` to override; orphan refs render as invalid CSS until pages are re-saved through VB |

The codes below are recorded inside `summary.errors[].code`, not as envelope codes. The first three refuse one page and let the batch continue; the fourth stops the run (see its row). Any of them makes the envelope `preset.reassign_partial_failure`:

| Per-page code | Tool | Meaning |
|---|---|---|
| `preset.round_trip_lossy` | `diviops_preset_reassign` | Parsing and re-serializing this page is not byte-identical, so a rewrite of its parsed tree cannot be told apart from the bytes the round trip itself drops. Carries `round_trip = { original_bytes, reserialized_bytes, byte_delta }`. Reported in dry-run too, so a plan never promises a rewrite the apply would refuse |
| `preset.global_layout_drift` | `diviops_preset_reassign` | The write would materialize a `divi/global-layout` wrapper's resolved content into the page (#11). Retry through the REST API / MCP server, or edit the referenced global layout directly |
| `preset.page_not_editable` | `diviops_preset_reassign` | The current user cannot `edit_post` this page — the per-post capability re-check inside the loop, on top of the route-level gate |
| `preset.rollback_storage_failed` | `diviops_preset_reassign` | This page's rollback chunk could not be stored (#199). **Unlike the three above, this one is not per-page: the run STOPS.** Continuing would grow the very insert that just failed while writing pages whose recovery data does not exist, so every remaining page is reported unattempted under this same code. The one place where continuing is worse than stopping |

**`error.data` extension.** Tools whose failure modes carry machine-readable detail beyond a code/message pair attach a structured `data` field on the error envelope. Today: `meta_wp_cli.command_failed` (`{ exit_code, stdout, stderr }`), `template_get` not_found (`{ available: string[] }`), `canvas_duplicate` conflict (`{ existing_canvas_id, parent_page_id }`), `library_save` conflict (`{ existing_library_id, layout_type, scope }`), `variable_delete` conflict (`{ id, ref_count, locations: object[] }` — see "Conflict payload: structured collections" below for the `locations` element union), `global_color_delete` conflict (`{ id, ref_count, used_in_posts: object[] }` — pass-through from Divi's `usedInPosts` index, distinct from `variable_delete.locations[]`), `preset_create` conflict (`{ existing_preset_id, bucket, bucket_key, name }`), `preset_delete` conflict (`{ preset_id, type, module, name, reason: 'is_default' }` — `reason` is a discriminator field leaving room for future conflict reasons), `preset.bucket_mismatch` / `preset.scope_mismatch` (`{ old_bucket | scope, new_bucket }`), `preset.too_many_pages` (`{ received, max_pages }`), `rollback_snapshot.run_restore_partial` / `rollback_snapshot.run_restore_failed` (`{ snapshot_id, kind, run_id, dry_run, requested, restored: object[], refused: object[], force }` — `refused[]` elements are `{ id, reason, detail, ... }` where `reason` is one of `conflict` / `unrestorable` / `already_restored` / `forbidden` / `not_found` / `unsupported_target` / `write_failed` / `readback_failed` / `side_effect_readback_failed`), `variable.fluid_generation_failed` (`{ min, max, targets, reason }`), `variable.fluid_system_generation_failed` (`{ profile, categories, reason }`), `variable.customizer_default_immutable` (`{ id, managed_by }`); the field is optional and absent on errors that don't need it. The `variable_*`, `global_color_*`, and `preset_*` namespaces also collapse input-shape rejections onto `invalid_input` with structured `error.data` documenting the failed field — see the adoption-status rows for the per-field shape (e.g. `{ field, expected, allowed?, received? }` for single-field rejections; `global_color_*` write paths additionally annotate `colors_index` so batch callers can identify which entry failed; `preset_create` / `preset_reassign` multi-field validation use `{ missing: [...] }`, `{ rejected_field, received, missing }`, or `{ requires: [...] }` shapes consistent with the variable_* namespace precedent). Per-tool descriptions list the `data` shape so consumers know what to expect per code without runtime probing.

**Conflict payload: structured collections.** When a `conflict` envelope carries a structured collection in `error.data`, document the element-level shape inline. The reference shape is `variable_delete.locations[]` — a discriminated union by `type`:

```ts
locations: Array<
  | { type: "page" | "post" | "et_header_layout" | "et_body_layout"
        | "et_footer_layout" | "et_pb_layout" | "et_pb_canvas",
      post_id: number, title: string }
  | { type: "preset", bucket: "module" | "group",
      module: string, preset_uuid: string, preset_name: string }
>
```

Future conflict envelopes carrying structured collections should mirror this shape (discriminator first, kind-specific context after).

**Adoption status today.**

| Namespace | Status | Tools |
|---|---|---|
| `schema_*` | ✅ adopted | `schema_list_modules`, `schema_get_module`, `schema_get_settings` |
| `meta_*` | ✅ adopted | `meta_ping`, `meta_info`, `meta_find_icon`, `meta_flush_cache`, `meta_wp_cli` |
| misc (server-local-leaning) | ✅ adopted | `render_preview`, `validate_blocks` |
| `template_*` | ✅ adopted | `template_list`, `template_get` (server-local; missing `template_name` returns `not_found` with `error.data.available: string[]`) |
| `tb_*` | ✅ adopted | `tb_template_list`, `tb_layout_get`, `tb_layout_update`, `tb_template_create`, `tb_template_trash` (missing `layout_id` / `template_id` returns `not_found`; missing Theme Builder master post returns `wp_error`; `tb_template_trash` default trash mode is idempotent silent-success on already-clean targets — `data.already_trashed: true` — and pre-checks per-layout/template status to skip already-trashed targets on partial-cleanup retries; `force=true` is one-shot (retry returns `not_found` because the post is gone from the DB); per-step failures return the namespaced `tb_template.command_failed` (HTTP 500) with `error.data.failed_step` ∈ { `layout_destroy`, `template_destroy`, `meta_scrub` }) |
| `canvas_*` | ✅ adopted | `canvas_list`, `canvas_get`, `canvas_create`, `canvas_update`, `canvas_duplicate`, `canvas_delete` (missing `canvas_post_id` / `parent_page_id` returns `not_found`; `canvas_duplicate` explicit-title collision returns `conflict` with `error.data = { existing_canvas_id, parent_page_id }`; empty `canvas_update` payload + malformed `canvas_id` / non-string content / out-of-range `append_to_main` return `invalid_input`) |
| `library_*` | ✅ adopted | `library_list`, `library_get`, `library_save` (missing `item_id` returns `not_found` with hint to `library_list`; `library_save` collision on `(title, layout_type, scope)` returns `conflict` with `error.data = { existing_library_id, layout_type, scope }`; missing title / non-string content / invalid `layout_type` or `scope` return `invalid_input`) |
| `variable_*` | ✅ adopted | `variable_list`, `variable_create`, `variable_create_fluid_system`, `variable_delete`, `variable_scan_orphans`, `variable_used_on_page`. Most error-vocabulary-rich namespace in the rollout: 16 input-shape rejection paths collapse onto `invalid_input` with structured `error.data` (single-field `{ field, expected, allowed?, received? }`; multi-field `{ conflict: [...] }` / `{ missing: [...] }` / `{ rejected_field, missing }` / `{ requires: [...] }`). Three namespace-prefixed extensions preserve algorithmic vs input distinctions: `variable.fluid_generation_failed` (clamp() math failed on syntactically-valid input — `error.data = { min, max, targets, reason }`), `variable.fluid_system_generation_failed` (`error.data = { profile, categories, reason }`), `variable.customizer_default_immutable` (`error.data = { id, managed_by }`). `variable_delete` with live references returns `conflict` (HTTP 409) with `error.data = { id, ref_count, locations: object[] }` — third concrete `conflict` adoption. `variable_delete` / `variable_used_on_page` / `variable_scan_orphans` missing-target paths return `not_found` |
| `global_color_*` | ✅ adopted | `global_color_list`, `global_color_create`, `global_color_update`, `global_color_delete`. Input-shape rejections (mode outside `[merge,replace]`, non-array `colors` payload, malformed gcid charset/length, non-CSS color value, missing `color` on a new entry, malformed `gcid` on delete) collapse onto `invalid_input` with structured `error.data` carrying `{ field, expected, allowed?, received? }` — multi-entry create/update calls additionally annotate `colors_index` so batch callers can identify which entry failed. Customizer-bound color defaults (gcid-primary-color / gcid-secondary-color / gcid-heading-color / gcid-body-color / gcid-link-color) reject with `variable.customizer_default_immutable` (HTTP 403) on both `global_color_update` and `global_color_delete` — same code as `variable_delete` because the identity is identical (5.4+ unified gcid-* into the variable manager but kept the customizer-binding for the five legacy defaults). `global_color_delete` with live references returns `conflict` (HTTP 409) with `error.data = { id, ref_count, used_in_posts: object[] }` — fourth concrete `conflict` adoption. `used_in_posts` is pass-through from Divi's `usedInPosts` index (Divi-maintained on VB save), NOT the discriminated-union `locations[]` shape `variable_delete` builds via parse_blocks; different field name reflects the provenance distinction. Missing gcids return `not_found`. `global_color_update`'s underlying upsert is merge-mode — supplying an unknown gcid that satisfies the charset rules creates a new color rather than failing as `not_found`; pre-check via `global_color_list` if you need strict-update semantics. NOTE: `diviops_global_color_create` is the de-facto correct `add_global_color` writer because Divi's own AI-Agent bundle has a Zod schema gap that drops the `label` field on writes (declared permanent through 7 Divi releases — see CLAUDE.md "Divi 5 upstream workarounds") |
| `global_font_*` | ✅ adopted | `global_font_list`, `global_font_create`, `global_font_update`, `global_font_delete`. Mirrors `global_color_*` semantically — stores `gfid-*` records under `et_global_data.global_fonts` (parallel to `global_colors`). Distinct from `diviops_variable_*({type:"fonts"})` which writes Divi-native `gvid-*` font tokens to the variable manager. `_list` ALWAYS returns `{count, fonts}` normalized — never bare `false` (empty substrate emits `{count: 0, fonts: {}}`). `_create` is STRICT (unlike `global_color_*`'s merge-mode upsert): collisions on existing `gfid` return `conflict` (HTTP 409) with `error.data = { id, existing }`. `_update` is STRICT: unknown `gfid` returns `not_found` (HTTP 404) with `error.data = { id }` — use `_create` to add. Stored shape: `{ family, source, weights[], subsets[], label, fallback, status, lastUpdated }`; `source` enum is `'google'|'system'|'custom'`. Input-shape rejections (malformed id, invalid source enum, non-array weights/subsets, missing required `family`/`source` for new entry) collapse onto `invalid_input` with structured `error.data`. `_delete` with live references returns `conflict` (HTTP 409) — locations[] shape built via parse_blocks (parallel to `variable_delete`, NOT pass-through like `global_color_delete`'s `used_in_posts`); reference walker matches the quoted `$variable()$` token form (`"name":"gfid-..."`) across pages / TB layouts / library / canvas / preset registry — same shape as `variable_*` and `global_color_*` walkers. No customizer-bound `gfid-*` defaults exist to protect (Divi's customizer-bound font defaults live in plain `heading_font` / `body_font` options, outside this registry) |
| `preset_*` | ✅ adopted | `preset_audit`, `preset_scan_orphans`, `preset_create`, `preset_update`, `preset_delete`, `preset_set_default`, `preset_reassign`, `preset_cleanup`. Largest blast-radius adoption to date — the preset registry has the most complex semantics in the suite. `preset_create` runs a per-bucket name uniqueness check before mint; collisions in the same `(bucket, bucket_key)` return `conflict` (HTTP 409) with `error.data = { existing_preset_id, bucket, bucket_key, name }` — fifth concrete `conflict` adoption (after canvas_duplicate / library_save / variable_delete / global_color_delete). `preset_delete` against a registered default returns `conflict` (HTTP 409) with `error.data = { preset_id, type, module, name, reason: 'is_default' }` — the `reason` discriminator field leaves room for future conflict reasons (e.g. `referenced_in_chain`) without reshaping. Three namespace-prefixed extensions on `preset_reassign` preserve structurally-distinct rejections: `preset.bucket_mismatch` (cross-bucket swap, `error.data = { old_bucket, new_bucket }`), `preset.scope_mismatch` (caller-asserted scope disagrees with new_uuid's bucket, `error.data = { scope, new_bucket }`), `preset.too_many_pages` (`error.data = { received, max_pages }`, hint to chunk). Input-shape rejections across the namespace collapse onto `invalid_input` with structured `error.data` (single-field `{ field, allowed?, received? }`; multi-field `{ missing: [...] }` / `{ rejected_field, received, missing }` / `{ requires: [...] }`). Missing preset / unknown bucket surfaces as `not_found`. Legacy dry_run plans (`preset_cleanup` / `preset_reassign` / `preset_delete` / `preset_set_default`) keep their route-specific summary shape inside the envelope `data` slot — plan-shape standardization handled in a separate pre-existing dry_run wave |
| `page_*` | ✅ adopted | `page_list`, `page_get`, `page_get_layout`, `page_create`, `page_update_content`, `page_update_meta`, `page_update_status`, `page_trash`. Highest-frequency mutating surface in the suite — page-content edits are the bread-and-butter authoring path. Missing post_id on any tool returns `not_found` with `error.data = { page_id }` and a hint to `diviops_page_list`. Edit-permission failures (`page_update_content`, `page_update_meta`, `page_update_status`) and delete-permission failures (`page_trash`) return `forbidden` (HTTP 403, kept distinct from `capability_missing` — the latter is a handshake-layer signal, the former is a row-level WordPress auth signal). Input-shape rejections collapse onto `invalid_input` with structured `error.data`: `page_create` / `page_update_content` non-string content → `{ field, received_type }`; `page_create` invalid status → `{ field, allowed, received }`; `page_update_meta` invalid title/slug/parent/menu_order or alias conflict → `{ field, ... }` including sanitized slug evidence where relevant; `page_update_status` enum violations → `{ field, allowed, received }`; `page_update_status` date_gmt validation → `{ field, received, requires?, min_lead_seconds? }`. `page_trash` is idempotent under repeat — a trash on an already-trashed post returns `ok:true` with `data.already_trashed = true` (repeat-safe semantics for AI-agent retries) rather than `conflict`; the `already_trashed` flag preserves the no-op signal for callers who need it. `page_update_meta` and `page_update_status` are similarly idempotent on same-value updates (`data.noop = true`). Legacy dry_run plans on `page_trash` and `page_update_status` keep their route-specific summary shape inside the envelope `data` slot — plan-shape standardization handled in a separate pre-existing dry_run wave |
| `module_*` + `section_*` | ✅ adopted | `module_update`, `module_move`, `module_lock`, `module_unlock`, `module_clone`, `section_get`, `section_append`, `section_replace`, `section_remove`. Last-wave adoption — closes the envelope rollout. Largest pre-adoption error-vocabulary in the suite (five legacy "search miss" codes — `module_not_found`, `block_not_found`, `section_not_found`, `no_match`, `unsupported_selector` — plus `parse_error`, `overlap`, `invalid_*`). All five search-miss variants collapse onto canonical `not_found` with a `target_kind` discriminator (`"module"` / `"section"` / `"block"` / `"page"`); the discriminator carries `target_mode` (`"label"` / `"match_text"` / `"auto_index"`) and `target_value` so callers branch on `error.data.target_kind` rather than the legacy code. `module_move` adds `error.data.context = "source" \| "target"` so the caller knows which selector failed when both are passed. Self-move (move-into-self) returns the namespace-prefixed `module.overlap` — semantically distinct from "search miss" and worth preserving signal. Field-level rejections (`invalid_position`, `invalid_attrs`, `invalid_auto_index`, `invalid_occurrence`, `missing_target`, `ambiguous_target`) collapse onto `invalid_input` with structured `error.data` (`{ field, allowed?, received }` / `{ reason, context? }`). `parse_error` (malformed Divi block markup) becomes `divi_error` (HTTP 500) with a hint to re-save through the Visual Builder. `forbidden` is kept distinct from `capability_missing` (same precedent as `page_*`). `section_remove` keeps 404-on-already-removed semantics — sections lack identity-preserving target modes (no UUID; label uniqueness is not guaranteed post-mutation; match_text re-resolves), so `_meta.idempotent: true` is preserved by the side-effect-equivalence path rather than the silent-success path used by `page_trash`. `module_update` dot-collision behavior (composable settings slots like `groupPreset["title.decoration.spacing"]`) preserved exactly — escape inner dots with `\.` to write the literal-dot key intact (`groupPreset.title\.decoration\.spacing.presetId`); the migration is response-shape-only, write semantics unchanged. Legacy dry_run plans on all 8 mutating tools keep their route-specific summary shape inside the envelope `data` slot |
| All others | ✅ adopted | Rollout complete — every namespace returns the standardized envelope on success and failure paths. |

All namespaces have adopted the envelope as of the last wave (`module_*` + `section_*`). Tool descriptions name the canonical envelope codes each tool can emit and the structured `error.data` shape that accompanies them.

**dry_run** plans (where supported) flow through the success branch as `data: { dry_run: true, plan: { summary, changes[, warnings] } }` — same shape as before, now wrapped in the envelope.

## Read Tools (27)
<!-- Distinct-tool count; multiple tools per bullet via `/` separator -->


- `diviops_meta_ping` — verify WordPress + plugin connection
- `diviops_meta_info` — DiviOps server identity, version, license type, capabilities
- `diviops_page_list` / `diviops_page_get` / `diviops_page_get_layout` — read pages (layout returns slim targeting metadata by default; use `full: true` for complete attrs)
- `diviops_schema_list_modules` / `diviops_schema_get_module` — discover modules and attributes (optimized schema by default). `diviops_schema_get_module({ mode: "dump_all" })` snapshots every Divi module + a `schema_version` hash + `divi_version` in one call — build-time entry point for the skill regen pipeline, not a normal authoring step
- `diviops_schema_get_settings` / `diviops_global_color_list` / `diviops_global_font_list` — site config. `diviops_global_font_list` always returns `{count, fonts}` even on empty substrates (never bare `false`)
- `diviops_meta_find_icon` — search 1,989 icons by keyword (returns unicode, type, weight)
- `diviops_section_get` — get a section's markup by admin label or text content
- `diviops_template_list` / `diviops_template_get` — load verified block markup templates
- `diviops_preset_audit` — audit presets with referenced/unreferenced analysis (exposes `block_ref_count`, `group_ref_count`, `referenced_by_presets` chain)
- `diviops_preset_inspect` — inspect one preset UUID: coordinates, all three attr bags, storage provenance, reference samples, geometry-scope leaks, and legacy/nested duplicates; read-only with no repair mode
- `diviops_preset_scan_orphans` — list page-referenced preset UUIDs missing from the D5 registry; separates dangling orphans from D4-legacy refs
- `diviops_library_list` / `diviops_library_get` — browse and load Divi Library items
- `diviops_render_preview` — render block markup to HTML for preview. Accepts EITHER `content` (inline markup) OR `page_id` (loads from DB; requires edit_post on the page). Use `page_id` for regression preview of shipped pages without round-tripping the markup blob.
- `diviops_validate_blocks` — validate block markup (structure, required attrs, known pitfalls). Accepts EITHER `content` (inline markup) OR `page_id` (loads from DB; requires edit_post on the page). Use `page_id` to validate shipped pages without round-tripping the markup blob.
- `diviops_tb_template_list` / `diviops_tb_layout_get` — browse Theme Builder templates and layouts
- `diviops_canvas_list` / `diviops_canvas_get` — browse and read off-canvas workspaces (popups, modals, menus)
- `diviops_variable_list` — list design token variables, filter by type (`colors`, `numbers`, etc.) or ID prefix. `prefix` matches the stored ID only; it does not match `label`. For semantic token names such as `oa-*` labels on UUID-backed Divi variables, list by `type` and filter the returned `label` client-side.
- `diviops_variable_scan_orphans` — find `gvid-`/`gcid-` refs with no backing Variable Manager entry (orphans render as invalid CSS when the `$variable()$` resolver falls through) plus variables defined but referenced nowhere (unused — deletion candidates). Scans pages, Theme Builder layouts (`et_header_layout` / `et_body_layout` / `et_footer_layout`), Divi Library items (`et_pb_layout`), canvas pages (`et_pb_canvas`), and the preset registry. Symmetric to `diviops_preset_scan_orphans`

## Write Tools (30)

- `diviops_preset_registry_doctor` — audit canonical D5 preset timestamps and chunk transients; guarded repair converts only parseable ISO timestamps after backup, with dry-run support

- `diviops_page_create` — create new page with Divi content
- `diviops_page_update_content` — full page rewrite
- `diviops_page_update_meta` — update title, slug, parent, and menu order without touching content. Slug changes on published posts preserve `_wp_old_slug` by default; use `diviops_page_update_status` for status
- `diviops_rollback_snapshot_restore` — restore one guarded snapshot to its captured post/page or Theme Builder layout. Requires target edit permission, refuses content or captured Divi post-meta drift before mutation, uses the full-content readback guard, and has no force path or second snapshot in this MVP
- `diviops_section_append` — add section to existing page (start or end)
- `diviops_section_replace` — replace section by admin label or text content
- `diviops_section_remove` — remove section by admin label or text content
- `diviops_module_update` — surgically update module attributes (dot notation, 3 targeting modes + occurrence)
- `diviops_module_move` — move any block to a new position (before/after a target block). Separate source + target targeting (auto_index, label, or text). Works across sections.
- `diviops_preset_cleanup` — manage presets: default (spam removal), `action=remove_orphans` with `scope=spam|all`, `action=rename_strip_prefix`, `dedup=true`
- `diviops_preset_create` — create a new preset in the D5 registry. Required: `module_name`, `name`, `attrs`. For `type: "group"` (attribute-level preset), also requires `group_name` (e.g. `divi/font`, `divi/button`) + `group_id` (e.g. `designTitleText`, `designButton`). Returns the created UUID as `preset.id` (nested under a `preset` object in the response). See [presets.md](presets.md) for the attrs-shape difference between `module` and `group` types
- `diviops_preset_reassign` — rewrite preset refs across pages from `old_uuid` → `new_uuid`. **`mode: "apply"` requires `page_ids`** — only dry-run scans the site, so plan first and apply with the ids the plan reported. Every applied page is written behind a mandatory rollback snapshot (id in `summary.details[].snapshot_id`, restore with `diviops_rollback_snapshot_restore`) and through the readback/revert integrity guard; pages that fail are refused individually and the envelope comes back `ok: false` with `preset.reassign_partial_failure`. Covers both module-level (`attrs.modulePreset[...]` arrays) and attribute-level (`attrs.groupPreset.<slot>.presetId`, scalar or array) refs, plus registry chain refs (`attrs.groupPresets.<slot>.presetId` in other presets that pull in `old_uuid`) for group-bucket swaps. `scope: "both"` (default) auto-selects based on `new_uuid`'s bucket; pass `"module"` or `"group"` to gate explicitly. Cross-bucket swaps (module ↔ group) are rejected. Legacy single-string form `"modulePreset": "uuid"` (D4-migrated content) is **not** rewritten — normalize to array form `["uuid"]` first if needed. `mode: "dry-run"` (default) or `"apply"`. `strip_inline: true` (default) — module-scope only — recursively removes per-attribute inline values that deep-equal the new preset's value at the same path; only fires when post-swap stack is singular `[new_uuid]`. Group-scope inline strip is not yet implemented (emits `summary.strip_advisory`). See [presets.md](presets.md) for full semantics
- `diviops_preset_update` / `diviops_preset_delete` — update or delete individual presets
- `diviops_library_save` — save block markup to Divi Library for reuse
- `diviops_tb_layout_update` — update Theme Builder header/footer/body content
- `diviops_tb_template_create` — create Theme Builder template with header/footer and conditions
- `diviops_tb_template_trash` — atomic cleanup: trash (or `force=true` permanently delete) a Theme Builder template + its linked header/body/footer layouts + scrub orphan `_et_template` meta refs on the master post. Closes the orphan-meta gap left by `page_trash` on linked layouts. Default trash mode is idempotent (silent-success on already-clean; pre-state checks skip already-trashed targets on partial-cleanup retry); `force=true` is one-shot. Supports `dry_run`
- `diviops_canvas_create` — create off-canvas workspace (popups, modals, menus) linked to a page
- `diviops_canvas_duplicate` — deep-copy a canvas (content + parent/append/z-index meta). Default copy title `<source> (Copy)` auto-suffixes on collision; explicit `title` collisions return 409. Supports `dry_run`
- `diviops_canvas_update` — update canvas content and/or metadata; pass any subset (e.g. `{canvas_post_id, title}` renames without touching content)
- `diviops_canvas_delete` — remove a canvas
- `diviops_variable_create` — create a design token variable (colors: `gcid-*` + hex, numbers: `gvid-*` + CSS value). For `type=numbers` fluid tokens, pass `min`+`max` shorthand (anchors default to 320px/1920px) or explicit `targets` like `{"320px":"20px","1920px":"60px"}` — server generates arithmetically-correct `clamp()` instead of hand-written math that silently under-reaches the stated max. All-px inputs emit px (root-agnostic). Rem inputs OR rem output require explicit opt-in: pass `output_unit="rem"` (accepts the 1rem=16px default) or `root_font_size_px:N` (declares the site's actual root, e.g. `10` for `html { font-size: 62.5% }`, `20` for `html { font-size: 20px }`). Mutually exclusive with `value`. For **`type="gradients"` (5.7.4)** pass the structured `gradient` object — `{stops:[{position,color},…], type ("linear"/"circular"/"elliptical"/"conic"), direction, directionRadial, length, repeat, overlaysImage}` — and the server serializes Divi's canonical `$variable({type:gradient,…settings…})$` token (the only renderable shape; see [presets.md → variable types](presets.md)). Do **not** pass a raw CSS gradient string in `value` — it's rejected as unrenderable. Requires diviops-agent ≥ 1.5.4 / server ≥ 1.5.28 (capability `variable_create_gradient`)
- `diviops_variable_delete` — delete a variable by ID (auto-detects storage from prefix). Refuses with HTTP 409 when live references exist unless `force=true`; run `diviops_variable_scan_orphans` to find where they live. Returns HTTP 403 for Divi's customizer-bound defaults (`gcid-primary-color`, `gcid-secondary-color`, `gcid-heading-color`, `gcid-body-color`, `gcid-link-color`) — those are managed via WP Customizer theme options, not this tool
- `diviops_global_font_create` / `diviops_global_font_update` / `diviops_global_font_delete` — DiviOps-managed global font catalog with `gfid-*` IDs (parallel to `gcid-*` colors), stored under `et_global_data.global_fonts`. Use to bind typography presets to canonical font-family slugs (e.g. `gfid-oa-sora`) rather than re-declaring `family` inline across every preset. Strict semantics: `_create` collisions return `conflict` (HTTP 409); `_update` against unknown gfid returns `not_found` (HTTP 404); `_delete` with live `gfid-*` references in pages / TB layouts / library / canvas / preset registry returns `conflict` (HTTP 409, locations[] payload) unless `force=true`. Distinct from `diviops_variable_*({type:"fonts"})` which writes Divi-native `gvid-*` font tokens to the variable manager — `global_font_*` is the DiviOps catalog presets bind via `gfid-` slugs

## Utility Tools (2)

- `diviops_meta_wp_cli` — run WP-CLI commands (allowlisted; default safelist + opt-in extended commands via `DIVIOPS_WP_CLI_ALLOW`; requires `WP_PATH` for Local by Flywheel or `WP_CLI_CMD` for containerized envs). Filesystem-touching commands (`wp export`, `acf export/import`) additionally require path arguments under a safe root (default `<WP_PATH>/.diviops-tmp/`; override via `DIVIOPS_WP_CLI_SAFE_FS_ROOT`; disable via `DIVIOPS_WP_CLI_UNSAFE_FS=1`). `wp export` requires explicit `--dir=` or `--stdout`. In `WP_CLI_CMD` wrapper mode, `DIVIOPS_WP_CLI_SAFE_FS_ROOT` is mandatory for FS-sensitive commands (host-derived paths don't correspond to the container namespace)
- `diviops_meta_flush_cache` — flush Divi's compiled CSS cache at `wp-content/et-cache/`. Needed after preset / variable / module mutations because `wp cache flush` does NOT invalidate this on-disk cache — stale CSS keeps serving until the cache is cleared. Delegates to Divi's native `ET_Core_PageResource::remove_static_resources` when available (response field `backend: "divi_native"`); native mode additionally clears Theme Builder CSS scattered across other post dirs, archive / taxonomy / home / notfound CSS, object cache, module features cache, post features cache, dynamic assets cache, Google Fonts cache, and post meta caches — significantly broader than the fs fallback. Falls back to a targeted filesystem walk of numeric-named subdirs when the Divi class is absent (`backend: "fs_fallback"`). Exactly one selector required: `post_id` (single post), `all` (every cached file), or `after` (unix ts — delete Divi CSS files with mtime strictly greater than ts; native backend does a single-pass sweep of the full et-cache tree preserving Visual Builder `-vb-*` runtime CSS, fallback iterates numeric post dirs whose latest file mtime exceeds the cutoff). No default to `all` — omitting a selector returns HTTP 400 to prevent accidental site-wide flushes. Safety in fallback mode: only numeric-named subdirs are touched; siblings like `.cache-cleared-at`, `global/`, `en_US/`, `notfound/`, `*.data` are never removed. Response includes `mode`, `backend`, `flushed`, `files_freed`, `bytes_freed`, and a `scope_note` in native mode describing the exact scope flushed per selector: `post_id` counts reflect the per-post dir only (lower bound — native clearer also removed matching TB CSS across other dirs); `all` covers numeric post dirs + archive/taxonomy/home/notfound/global subtrees; `after` sweeps the whole et-cache tree by file mtime and counts include non-post subtree deletions, so `files_freed` is not limited to per-post dirs. Idempotent when cache root missing (returns 200 with empty list)

## `dry_run` for Divi-builder writes

Every Divi-builder write tool accepts `dry_run: boolean` per the [diviops/](../../diviops/SKILL.md) primer's plan-shape contract. Use it to:

- Preview a `module_update` / `section_replace` / `preset_update` change before committing.
- Confirm a `library_save` / `tb_template_create` / `page_create` would create the post you expect (no title-uniqueness guard yet — see per-tool descriptions for caveats).
- See which post dirs `meta_flush_cache` would actually touch.

`diviops_meta_wp_cli` does not accept `dry_run` (raw passthrough); see the primer for the general exception list.

## Unknown Native Setting VB Calibration (advisory workflow)

When the native VB setting path is unknown, run this VB calibration loop before shipping a DiviOps attr write or falling back to CSS:

1. Capture the baseline: `diviops_page_get_layout` or `diviops_tb_layout_get` with full attrs, plus desktop and phone screenshots for the relevant frontend state.
2. Ask the project lead to make exactly one Visual Builder setting change and save. Keep every other setting untouched.
3. Capture the same attrs again and diff the before/after block JSON.
4. Map the VB path to the DiviOps attr path, including breakpoint and state keys. Record the evidence tier from `SKILL.md`.
5. Apply the mapped attr through the relevant DiviOps write tool, verify the frontend, then document the pattern in the skill docs if it is reusable.

Use this loop for native Theme Builder settings such as `Disable On` visibility, footer spacing, or any Composable Settings path that is not already VB-verified. Stamp the discovered mapping `VB-verified` only after the saved Visual Builder diff preserves the attr path.

## Targeting Reference

### Module targeting (`diviops_module_update`)

Three targeting modes, in priority order:

| Mode | Parameter | Example | Use when |
|---|---|---|---|
| **Auto-index** | `auto_index: "text:5"` | `diviops_module_update(page_id: 312, auto_index: "text:5", attrs: {...})` | Any module — get the index from `diviops_page_get_layout` |
| **Admin label** | `label: "Hero Heading"` | `diviops_module_update(page_id: 312, label: "Hero Heading", attrs: {...})` | MCP-generated content with labels |
| **Text match** | `match_text: "Kitas"` | `diviops_module_update(page_id: 312, match_text: "Kitas", attrs: {...})` | Quick targeting by visible text content. Use `auto_index` instead for generic/repeated terms; content-slot mismatches (for example heading `title.innerContent` on a matched `divi/text`) return `invalid_input` instead of silently writing unused attrs. |

**Duplicate handling**: Add `occurrence: N` (1-based) when multiple modules share the same label. Response includes `total_matches` when duplicates exist.

### Section targeting (`diviops_section_get`, `diviops_section_replace`, `diviops_section_remove`)

| Parameter | Example | Use when |
|---|---|---|
| `label` | `label: "Hero"` | Section has an admin label |
| `match_text` | `match_text: "Lernen, das sich"` | Find section by text content (case-insensitive substring) |
| `occurrence` | `occurrence: 3` | Multiple sections match (1-based) |

Either `label` or `match_text` is required. When duplicates exist, `diviops_section_get` includes `total_matches` and a warning; `diviops_section_replace` / `diviops_section_remove` include `total_matches`.

#### Recipe: insert a section mid-page (composite workflow)

`diviops_section_append` only places sections at the start or end of a page. To insert a new section between two existing sections, this composite workflow composes the read and replace tools instead of adding a wrapper tool. If multiple sections match the target label or text, pass the same `page_id` and `occurrence` index to both `diviops_section_get` and `diviops_section_replace` so the read and write target the same section on the same page.

For either insertion direction, preserve the captured original section markup exactly. Do not reconstruct the existing section from memory, and do not retry before checking whether the new section was already inserted.

Insert before section B:

1. Use `diviops_section_get` on section B and preserve the returned markup verbatim.
2. Validate the combined markup (`new_section_markup + original_section_b_markup`) with `diviops_validate_blocks`. Abort the workflow if validation fails to prevent page corruption.
3. Use `diviops_section_replace` on section B with the validated combined markup.

The render order becomes section A → new section → original section B. Section A is untouched.

Insert after section A:

1. Use `diviops_section_get` on section A and preserve the returned markup verbatim.
2. Validate the combined markup (`original_section_a_markup + new_section_markup`) with `diviops_validate_blocks`. Abort the workflow if validation fails to prevent page corruption.
3. Use `diviops_section_replace` on section A with the validated combined markup.

The render order becomes original section A → new section → section B. Section B is untouched.

> Idempotency warning: this composition is not idempotent by default. Before retrying or running the recipe again, use `diviops_section_get` or `diviops_page_get_layout` to confirm the intended new section is not already present; otherwise the same insert can duplicate the new section or shift duplicate-match `occurrence` targeting.

### Auto-index (from `diviops_page_get_layout`)

The layout response includes targeting metadata per module:
- `admin_label` — manually set label
- `text_preview` — first ~50 chars of innerContent
- `auto_index` — type + sequential counter (e.g., `text:5`, `icon:3`) — works for ALL modules, including those without labels or text
